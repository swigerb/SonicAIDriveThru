# Beth — History

## 2026-09-23 — Joined the Sonic Squad

- Added by the coordinator for the C# / .NET 11 port (issue #4), with Brian's approval of the direction: keep the Python backend, add a feature-equivalent C# backend on .NET 11 RC 1, validate both with one shared conformance suite, deploy them side by side, and later unify the Sonic, McDonald's and Dunkin personas in both languages.
- Toolchain verified: SDK `11.0.100-rc.1.26425.128`, installed per-user at `%LOCALAPPDATA%\Microsoft\dotnet11` (not on PATH, so call it by full path). NuGet restores only through `packagefeedproxy.microsoft.io`.
- Key design rule inherited from the analysis: Python's asyncio gives each session single-threaded semantics that the code relies on implicitly. The C# port preserves them with one sequential event loop per session (a `Channel<T>` consumed by one loop), not locks.

## 2026-09-23 — feat/conformance-harness (#7, #11)

- Implemented `tests/conformance/` (Central Package Management via `Directory.Packages.props`, restoring only through `packagefeedproxy.microsoft.io`) with `Nullable=enable` + warnings-as-errors in every suite project.
- Confirmed xUnit v3 APIs assumed during scaffolding actually exist and work on `xunit.v3` against SDK `11.0.100-rc.1.26425.128`: `Assert.Skip(string)`, `Xunit.TestContext.Current.CancellationToken`, `IAsyncLifetime` with `ValueTask`.
- Found and fixed a WebSocket server-side close-handshake bug in `FakeRealtimeUpstreamServer`: `socket.State` is `CloseReceived` (not `Open`) right after receiving a client close frame, so a naive `state == Open` guard skipped completing the handshake and the client's `CloseAsync()` threw. Fixed by accepting `Open or CloseReceived` and catching `WebSocketException` best-effort.
- Fixed `HealthEndpointTests` to parse JSON instead of raw-substring-matching `"status":"healthy"` (the real payload has spaces).
- Cross-platform fix for CI (#11): `RepoPaths.PythonExecutable()` was hardcoded to `.venv/Scripts/python.exe`; branched on `OperatingSystem.IsWindows()` since GitHub-hosted Linux runners use `.venv/bin/python`. No regression: 8/8 green ×3 on Windows after the change.
- `dotnet test tests/conformance` green 3× in a row at multiple checkpoints; `.gitignore` correctly excludes `bin/`/`obj/`.
- Commits: `817f487` (harness + smoke scenario), `9f8188a` (cross-platform fix).
- Decision logged: `.squad/decisions/inbox/beth-dotnet-harness-implementation.md`.

## 2026-09-24 — feat/conformance-harness Stage A (Rick's PR #22 review, #7/#11)

- Implemented Rick's "must fix before merge" items 1–7 + 19, one commit per item (items 2+3 share one engine commit with an explicit rationale, since both touch the same `RespondAsync` method and item 3 subsumes item 2's requirement):
  - **1** (`1e33e5f`): CI now runs `npm ci && npm run build` in `app/frontend` before pytest and `dotnet test`; the harness itself now fails fast with a clear message (`'...\static\index.html' does not exist...`) instead of an opaque "backend exited early" when the frontend hasn't been built.
  - **7** (`ee9fc91`): `FindRepoRoot` now works in git worktrees — accepts `.git` as either a directory or a file (worktree pointer), falls back to marker files.
  - **19** (`7d134d1`): moved `global.json` to the repo root pinning `11.0.100-rc.1.26425.128` with `rollForward: latestPatch`; removed the duplicate under `tests/conformance`.
  - **6** (`89a3c8c`): the deflate scenario now sets `CollectHttpResponseDetails = true` so it can actually observe (and fail on) negotiated compression; mutation-checked by temporarily forcing `ws_compression: true` in the Python config — the test failed as expected, reverted, green again.
  - **5** (`0371029`): replaced the shared server-level frame log in `FakeRealtimeUpstreamServer` with per-connection `FakeRealtimeConnection` (id, own frame log, api-key, model query param) tracked by a new `ConnectionRegistry` exposing `WaitForNextConnectionAsync`/`WaitForNoOpenConnectionsAsync`; rewrote `SmokeSessionBootstrapTests` onto the new API.
  - **4** (`28e56d3`): `RealtimeSessionState.AssistantAudioSeen` is now set the moment the fake emits the first `response.output_audio.delta`; added a self-test proving `cannot_update_voice` actually fires once audio has been sent (was previously wired in the validator but never exercised by a scripted end-to-end response).
  - **2+3 engine** (`3de354e`): rewrote `RespondAsync` to emit the full GA response item lifecycle — `response.output_item.added` → `conversation.item.added` → `response.content_part.added` → (audio deltas | function-call-arguments.done) → `response.content_part.done` → `response.output_item.done` → final `response.done` carrying accumulated `output[]` and a new `BuildUsage()` GA-shaped `usage` object; every event now carries `event_id`, item-scoped events carry `item_id`/`output_index`/`content_index`. Added `RealtimeSessionState.LastConversationItemId` for `previous_item_id` chaining.
  - **2 scenario** (`688d713`): new `ResponseDoneRoundTripTests` proves the browser receives `extension.round_trip_token` and the backend logs no traceback past the greeting. Mutation-check reproduced the real `rtmt.py` bug this guards against: `response.done` without `output`/`usage` throws `KeyError: 'output'` inside `_process_message_to_client`, caught by `_forward_messages`'s outer handler, which silently kills both directions of the WebSocket forwarding — exact traceback captured and pasted into the commit message.
  - **3 scenario** (`bfdce90`): new `UpdateOrderToolCallTests` proves a scripted `update_order` function call round-trips correctly: `function_call_output` reaches the fake upstream for the right `call_id`, and `extension.middle_tier_tool_response` reaches the browser client.
- Investigated `rtmt.py`/`tools.py` read-only to ground items 2/3/4 in the real GA contract: confirmed the unconditional `message["response"]["output"]` at line ~874 is the exact root cause item 2 targets; confirmed `update_order`'s argument schema (`action`, `item_name`, `size`, `quantity`, optional `price`) and that a raw client `response.create` passes through `_process_message_to_server` unchanged (only `session.update` is specially handled), enabling deterministic test-driven second turns without simulating audio/VAD timing.
- Full clean-state re-validation after restoring `app/backend/static` (moved aside earlier to prove item 1's fail-fast message): `dotnet test tests/conformance` 3× green (14/14 each, ~1s), `pytest app/backend/tests -q` 586 passed/61 subtests, `ruff check .` clean, `npm test` 116 passed, `git status` clean, no `bin/`/`obj/` tracked.
- Extension points left for Stage B: per-connection scripting (item 8) — `Script`/`AutoRespond`/`QueuedResponses` are still server-level by design, safe today only because xUnit's collection fixture serializes tests in `ConformanceCollection`; `usage` values in `BuildUsage()` are plausible but not GA-exact since no test currently asserts precise token counts.
- Decision logged: `.squad/decisions/inbox/beth-stage-a-ga-lifecycle.md`.

## 2026-09-24 — feat/conformance-harness Stage B item 8 (#7)

- Moved the scripting surface from server-level to per-connection (`063f7cb`): `FakeRealtimeConnection` now owns `Script` (a fresh `RealtimeScript.WithVadDefaults()` per connection), `SessionState`, a send lock (`SendAsync` behind a `SemaphoreSlim`), `CloseAsync`/`Abort`, `TimeProvider`, and `NewEventId()`. Two-phase construction — `ConnectionRegistry.Add(...)` runs *before* `AcceptWebSocketAsync`, `AttachSocket(socket)` runs after — preserves the pre-existing guarantee that a test's own `ConnectAsync` returning means the connection is already visible to `WaitForNextConnectionAsync`; got this backwards once (registering after accept) and hit a real race that hung a test, fixed by restoring the original ordering.
- `RealtimeScript` gained rule-based triggers: `On(predicate, handler)` plus a `WithVadDefaults()` factory registering two default rules — `input_audio_buffer.append` fires the `speech_started → speech_stopped → committed → transcription.completed` sequence immediately (VAD-*like*, not VAD-*faithful*: no real silence delay, since simulating it buys nothing black-box and adds non-determinism), and `conversation.item.create` acks with `conversation.item.added`. Deleted the dead `RawErrorEvent` record; added `AudioDeltaEvent.Pace` for barge-in pacing.
- `FakeRealtimeUpstreamServer`: added `RejectNextConnectionWith(statusCode)` — a FIFO one-shot queue checked before `AcceptWebSocketAsync`, so a queued rejection fails the handshake with a raw HTTP status instead of completing the 101 upgrade (necessarily server-level: no connection object exists yet). Rewrote the receive loop to be non-blocking: each frame is dispatched to a fire-and-forget task tracked in an `outstanding` list (drained via `Task.WhenAll` once the socket loop exits), so a still-streaming `response.create` and new incoming frames are handled concurrently, serialized only at the socket-write level by `SendAsync`'s semaphore.
- New tests (`FakeRealtimeScriptingModelTests.cs`, 6 total incl. a 2-case theory): handshake rejection (401/429) asserting `ClientWebSocket.HttpStatusCode` via `Options.CollectHttpResponseDetails = true`; one-shot (not sticky) rejection; VAD-default ack ordering; `conversation.item.create` ack; per-connection isolation (connection A's cleared rules / queued failure must not leak onto fresh connection B).
- While mutation-checking, found frame waits in the codebase rely solely on `TestContext.Current.CancellationToken` with no bounded timeout on `WebSocketJson.ReceiveJsonAsync` — a real regression hangs the test host instead of failing (confirmed: had to use `--blame-hang-timeout` to unstick a `dotnet test` invocation). Added a local `ReceiveJsonWithTimeoutAsync` helper (10s linked `CancellationTokenSource`) in the new test file so regressions there fail cleanly; flagging this as a gap the shared `WebSocketJson` helper should probably close in a later item, since every existing test file has the same exposure.
- Mutation-checked: disabled the VAD rule's predicate → the VAD-sequence test fails with a clean `TimeoutException` in 10s (restored → green); made `RejectNextConnectionWith` a no-op → all 3 rejection tests fail with "no exception was thrown" (restored → green).
- `dotnet test tests/conformance`: 20/20 passing, run 3× green (one earlier red run in the same shell session was a one-off flake from back-to-back filtered invocations, not reproducible in 3 consecutive full runs).
- Commit: `063f7cb`.

## 2026-09-24 — feat/conformance-harness Stage B item 9 (#7)

- Re-derived the GA `session.update`/client-event validation surface independently of `app/backend/rtmt.py` (only read `rtmt.py` afterwards, as a cross-check, never as the source): fetched the official OpenAI GA realtime API reference (`https://developers.openai.com/api/reference/resources/realtime`, 2026-09-24) into a local scratch copy and grepped it for the `Realtime Session Create Request`, `Session Update Event`, `Realtime Error`/`Realtime Error Event`, and `Realtime Client Event` sections. Confirmed the existing 14 top-level `session` keys independently (reassuring, not coincidental) and pulled the `audio.input`/`audio.output` nested-shape allow-lists and an 11-member client-event-type union straight from the doc.
- Ran **one live probe** against the real Azure OpenAI GA realtime endpoint (subscription `44847a42-6b69-4e6c-b7e5-ce7140469dd6`, `wss://cog-axgpampkq3yfa.openai.azure.com/openai/v1/realtime?model=gpt-realtime-2.1`, bearer via `az account get-access-token`), per the coordinator's explicit one-probe authorization, and recorded four findings (full detail + honest "not independently verified" callouts in `tests/conformance/README.md`):
  1. Unknown top-level session key → `error.code = "unknown_parameter"` (not the coarse `error.type`) — **this caught a real bug**: the fake was using `"invalid_request_error"` as the specific code. Fixed.
  2. Missing `session.type` → `error.code = "missing_required_parameter"`, `param = "session.type"`.
  3. Unknown top-level client event type → `error.code = "invalid_value"`, message enumerating the service's own exact 12-member supported-type list — which includes `session.close`/`transcription_session.update` (not obvious from doc prose alone) and omits `output_audio_buffer.clear` (which the doc does list). Kept the union of both (13 entries) so nothing either source promises is wrongly rejected.
  4. Missing `Authorization` header → plain HTTP 401 at the handshake, no JSON frame.
  Did **not** attempt to live-verify `cannot_update_voice` or reasoning-on-1.5 codes (would need session/model state unreachable in one scripted probe) — said so explicitly in code comments and the README instead of guessing, per instruction.
- `GaSessionValidator.cs`: rewrote the class doc with full citations; added `AudioInputKeys`/`AudioOutputKeys` and nested-key rejection (new); added `GaClientEventTypes` + `ValidateClientEventType()` (new); fixed the `unknown_parameter` code bug above; added a `session.type`-required check, ordered *last* (after unknown-key/reasoning/cannot_update_voice) so it only fires when nothing more specific already rejected the frame — required because several existing hand-crafted test payloads omit `type` and rely on an earlier rule firing, and because real Python-driven E2E traffic is unaffected (`rtmt.py`'s `_to_ga_session()` always sets `type: "realtime"`, confirmed by reading, not editing, the file).
- `FakeRealtimeUpstreamServer.cs`: dispatch now rejects unrecognised top-level client event types before any rule/switch runs (extracted a shared `SendValidationErrorAsync` helper so `session.update` and this new check share one error-JSON code path). Added opt-in `RequireApiKey`/`ExpectedApiKey` (default off — zero existing raw-connect tests set an `api-key` header, so default-on would have broken ~15 of them) with a 401 handshake rejection when enabled; wired on only for `ConformanceFixture`'s shared Python-backed connection (safe: `rtmt.py` always sends the correct key).
- `BackendEnvironment.cs`: extracted the two hardcoded key literals into named `OpenAiApiKey`/`SearchApiKey` constants so the fixture and the environment builder can't drift.
- 9 new tests in `FakeRealtimeScriptingModelTests.cs` (missing `session.type`; unknown top-level key — added specifically to guard the bug fix, since nothing previously asserted that code; nested `audio.input`/`audio.output` unknown-key rejection ×2 theory cases; unknown client-event-type rejection; 401 missing/wrong api-key ×2; 200 correct-key accepted); fixed two pre-existing hand-crafted payloads in `FakeRealtimeScriptingTests.cs` that needed `session.type` added to keep passing under the new required-type rule.
- Mutation-checked every new rule by disabling it in isolation and confirming a clean, specific failure, then restoring to green: `session.type` check (assertion mismatch), nested audio-key check (both theory cases threw `KeyNotFoundException` — no error frame arrived), client-event-type check (bounded 10s timeout — dispatch silently proceeded), api-key check (both 401 tests got "no exception was thrown"), and the `unknown_parameter` code fix itself (reverting it to the old `"invalid_request_error"` value failed the new guard test with a clean string-mismatch).
- Wrote `tests/conformance/README.md`'s "GA validation fidelity — live-probe evidence" section (doc + probe citations, the four recorded findings, and the two explicit non-verified callouts) — this is the first content in that file; a later item will add the full `BackendContract` table.
- Cleaned up all research scratch files (`_ga_realtime_ref.md`, `_live_probe{,2,3}.py`) before commit — none contained secrets (bearer token only ever lived in an environment variable) but none belonged in the repo either.
- `dotnet test tests/conformance`: 28/28 passing, 3× green.
- Commit: `0afdc7e`.

## 2026-09-24 — feat/conformance-harness Stage B item 10 (#7)

- GA `session.update` is a partial patch, not a replacement, so `session.updated` must echo the full *accumulated* effective session across every prior update on that connection, not just the latest delta. Added `RealtimeSessionState.EffectiveSession` (a mutable `JsonObject`) and `MergeSessionUpdate(JsonElement)` implementing that merge: top-level keys overwrite; `audio.input`/`audio.output` merge one level deeper so setting `audio.input.format` in a second update doesn't drop `audio.output.voice` set by a first. `FakeRealtimeUpstreamServer.HandleSessionUpdateAsync` now calls the merge after validation passes and builds the `session.updated` response from an independent clone of `EffectiveSession` (round-tripped through `JsonNode.Parse(...ToJsonString())` so the transient server-assigned `id`/`object`/`model` overlay never mutates the stored state) instead of the previous stub `{id, object, model}`-only echo.
- New test `Session_updated_echoes_the_full_accumulated_effective_session_across_updates` (`FakeRealtimeScriptingModelTests.cs`): sends two sequential `session.update` frames (first sets `instructions`/`tool_choice`/`tools`/`audio.output.voice`; second only touches `audio.input.format`) and asserts the final `session.updated` still contains everything from both. Mutation-checked: guarded the `MergeSessionUpdate` call off → clean `KeyNotFoundException` (instructions missing from the second echo); restored → green.
- **Real Python finding** (not fixed here, per the coordinator's explicit instruction): read `app/backend/rtmt.py`'s `_process_message_to_client` (read-only) and confirmed it scrubs `instructions`/`tools`/`tool_choice`/`max_response_output_tokens` from `session.created` before forwarding to the browser (comment: "Hide the instructions, tools and max tokens from clients") but has **no equivalent case for `session.updated`**, and `session.updated` is also absent from `audio_pipeline.py`'s `_PASSTHROUGH_SERVER_TYPES` — so it falls through untouched and the full system prompt + tool schema are forwarded verbatim to the browser on every `session.updated` frame. Confirmed empirically, not just by code reading: wrote `SessionUpdatedClientVisibilityTests.Browser_never_receives_instructions_or_tools_in_session_updated` against the real Python backend via `ConformanceFixture`, ran it **without** `Skip` first, and it failed cleanly: `System.InvalidOperationException : The browser must never receive the system prompt via session.updated.` Restored `[Fact(Skip = "...")]` with a reason citing the exact rtmt.py behavior and this empirical result, referencing the follow-up issue the coordinator will file. **Flagging prominently for the coordinator: this is a real backend bug, not a harness gap — session.updated leaks the full system prompt and tool schema to any browser client.**
- `dotnet test tests/conformance`: 29 passed / 1 skipped / 30 total. Suite run 5× in a row green after this change; one earlier isolated failure immediately following a filtered single-test invocation was a port-release race between the filtered run's backend process and the next full run's — no orphaned `python.exe`/`dotnet` processes found afterwards, and 5 consecutive clean full runs since. Flagging as motivation for Stage B item 17 (process lifecycle), not yet implemented at this point in the branch.
- Commit: `9bd552f`.

## 2026-09-24 — feat/conformance-harness Stage B item 11 (#7)

- `RealtimeBrowserClient` hardened per item 11: added an `Origin` request header on the handshake (real browsers always send one, even same-origin; defaults to the backend's own HTTP origin since app/backend/static serves the built frontend same-origin in production — a real dev-server origin doesn't apply here). Added `CloseStatus`/`CloseStatusDescription` properties and a public `CloseAsync(status, description, ct)` for test-initiated graceful closes, plus `WaitForCloseAsync(timeout, ct)` — a `TaskCompletionSource` signaled from the reader loop's `finally` block, awaited with a bounded timeout instead of a sleep, so tests can assert on the close deterministically.
- Found and fixed a real concurrency bug while wiring this up: `ClientWebSocket.CloseAsync` performs its own internal receive loop while waiting for the server's answering Close frame, which races with the background reader loop's already-in-flight `ReceiveAsync` and throws "there is already one outstanding read call for this WebSocket instance." Switched both the new `CloseAsync` and `DisposeAsync` to `CloseOutputAsync` (send-only — no competing receive) and let the existing reader loop observe the server's answering Close frame naturally (it already treats a Close message as a null frame and exits, now also signalling `WaitForCloseAsync`). Also fixed `DisposeAsync` to drain the reader task before touching the socket at all, closing the same race on the teardown path (previously it called `CloseAsync` immediately after cancelling the reader, with no guarantee the in-flight receive had actually unblocked yet).
- Added typed send helpers matching every message type `app/frontend/src/hooks/useRealtime.tsx` actually sends (`input_audio_buffer.append`/`.clear`, `response.cancel`, `extension.set_verbose_logging`/`set_log_to_file`/`set_voice`/`end_session`/`resume`), plus `SendResponseCreateAsync()` for the one test-only pattern (`UpdateOrderToolCallTests`) that previously built a raw `JsonObject` inline.
- New test `BrowserClientLifecycleTests.Graceful_close_is_answered_by_the_backend_and_observed_via_WaitForCloseAsync`: connects, completes the greeting round trip, calls `CloseAsync`, awaits `WaitForCloseAsync`, and asserts the backend answered with `NormalClosure` — confirmed this is aiohttp's own `autoclose` protocol behavior (`rtmt.py`'s `from_client_to_server` loop just `break`s on `WSMsgType.CLOSE`, sends nothing itself). Mutation-checked: disabled the reader loop's close signal behind a non-constant guard → clean `TimeoutException` ("Socket did not close within 00:00:30") after the full 30s bound; restored → green.
- `dotnet test tests/conformance`: 30 passed / 1 skipped / 31 total, 3× green.
- Commit: `2f49af9`.

## 2026-09-24 — feat/conformance-harness Stage B item 12 (#7)

- `CONFORMANCE_TEST_HOOKS` and its overrides are read once at Python module-import time (e.g. `rtmt.py`'s `_SESSION_CONFIGURED_TIMEOUT_SEC = test_hooks.seconds(...)` is a module-level constant), so a running backend process can never switch profiles mid-suite — each profile needs its own dedicated Python process, hence its own xUnit collection.
- Added `BackendProfile`/`BackendProfiles` (`Conformance.Harness/BackendProfile.cs`): a named `ExtraEnvironment` dictionary layered onto the launcher. `Default` (hooks off — most of the suite), `ShortTimers` (hooks on, every currently-wired timer override collapsed to ~1s: idle/grace/nudge/greeting-timeout/rate-limit retries), `FixedClock(instant)` (hooks on, `CONFORMANCE_FIXED_NOW` set to an ISO-8601 instant with explicit UTC offset, timers left at production defaults).
- `ConformanceFixture` gained a `protected virtual BackendProfile Profile => BackendProfiles.Default;`, wired into `BackendLauncherFactory.StartAsync(..., extraEnvironment: Profile.ExtraEnvironment)`. New `ShortTimersConformanceFixture`/`ShortTimersConformanceCollection` and `FixedClockConformanceFixture`/`FixedClockConformanceCollection` (`BackendProfileFixtures.cs`) each override `Profile` and get their own Python process, following the existing `ConformanceFixture`/`ConformanceCollection` pattern exactly.
- `FixedClockConformanceFixture.Instant` is hardcoded to `2026-07-04T15:00:00-05:00` (America/Chicago, CDT) — inside `config.yaml`'s `happy_hour_start: 14`/`happy_hour_end: 16` window (store-local, matching `order_state.py`'s default `STORE_TIMEZONE`) — so the happy-hour proof test is deterministic regardless of what day or hour the suite actually runs on.
- Added `FakeRealtimeUpstreamServer.SuppressNextSessionUpdatedResponse()`: a one-shot, server-level FIFO counter (same pattern as the existing `RejectNextConnectionWith`), consumed atomically inside `HandleSessionUpdateAsync` *after* the session state is still validated/merged as normal — only the reply frame is swallowed. Armed before any connection exists, so there's no race with `WaitForNextConnectionAsync`: the suppression is consumed synchronously by whichever `session.update` the fake processes next, and nothing else could reach the fake first.
- **`GreetingTimeoutFallbackTests`** (ShortTimers collection): proves `CONFORMANCE_GREETING_TIMEOUT_SECONDS` actually shortens `rtmt.py`'s `send_greeting_once` fallback wait. Discovered while writing this that `session_configured` (rtmt.py) is set by *either* the bootstrap `session.update`'s reply or the browser's forwarded `session.update`'s reply — suppressing only the first still let the second's near-instant reply set it before any timeout could fire, so the greeting fired via the normal trigger instead of the fallback. Fixed by arming the suppression twice (once per reply). Asserts the greeting `response.create` still arrives within 3s despite both replies being suppressed — comfortably above the profile's ~1s override, comfortably below the 5s production default — and that the backend logged its own `"No session.updated within"` warning, so this can only pass via the fallback path, not by accident. Mutation-checked: temporarily set the profile's override to 10s (larger than the 3s test bound) — clean `InvalidOperationException` (timeout) with the honest inner backend log showing the greeting never fired within the bound; restored to `1` → green.
- **`HappyHourPricingTests`** (FixedClock collection): scripts an `update_order` "add" of `"Cherry Limeade"` (a name that doesn't need to exist in `menuItems.json` — `menu_utils.infer_category`'s keyword fallback matches `"limeade"` → `"drinks"`, which `order_state.py`'s happy-hour discount applies to) at `price=4.00`, and asserts the `extension.middle_tier_tool_response`'s `tool_result` (a JSON-serialised `OrderSummary`, per `tools.py`'s `client_text=json_order_summary`) has `finalTotal` == `4.00 * 0.5 * 1.08` == `2.16` (50% happy-hour discount, then 8% tax, both from `config.yaml`'s `business_rules`). Mutation-checked: temporarily moved `FixedClockConformanceFixture.Instant` to `10:00` (outside the 14:00–16:00 window) — clean `Assert.Equal` failure, actual value `4.32` (the exact undiscounted total), proving the assertion really depends on the frozen clock; restored to `15:00` → green.
- `dotnet test tests/conformance`: 32 passed / 1 skipped / 33 total (two new collections, each its own Python process — measured no meaningful runtime increase, still ~2s total). 3× green.
- Commit: `876bc83`.

## 2026-09-24 — feat/conformance-harness Stage B item 13 (#7)

- Split the previously-single `PythonBackendOptions` into a truly **neutral `BackendContract`** record (`Conformance.Harness/BackendContract.cs`: `RealtimeBaseUri`/`SearchBaseUri`/`Port`/`Deployment`/`Voice`/`SearchIndex`/`StoreTimeZone`, plus `const`s for the fixed API keys and every Azure AI Search field/config name) representing the facts *any* backend implementation needs to satisfy — Python today, the future .NET backend once S2 exists (issue #7) — versus a slimmed-down `PythonBackendOptions` (now just `ExtraEnvironment`) holding only this-launcher's own extras. `BackendEnvironment.Build(BackendContract, PythonBackendOptions)` now clearly comments which vars come from the neutral contract vs. which are Python-launcher-specific quirks (`RUNNING_IN_PRODUCTION`, `LOG_LEVEL`, `PYTHONUNBUFFERED`, `PYTHONUTF8`, `APP_SESSION_SECRET`, `RATE_LIMIT_RECOVERY_ENABLED`).
- `BackendLauncherFactory.StartAsync` kept its existing loose-parameter public signature (single call site: `ConformanceFixture.cs`) but now builds a `BackendContract` internally via `BackendContract.ForPort(...)` before dispatching to `PythonBackendLauncher.StartAsync(contract, options, ct)` — a future `DotnetBackendLauncher` would take the identical `BackendContract` and supply its own options type.
- Moved the `OpenAiApiKey`/`SearchApiKey` constants from `BackendEnvironment` onto `BackendContract` (their natural home — they're neutral facts about the contract, not Python-env-var-building logic) and updated `ConformanceFixture.cs`'s one reference (`Realtime.ExpectedApiKey = BackendContract.OpenAiApiKey`) plus doc-comment references in `FakeRealtimeUpstreamServer.cs`.
- **Fixed the real gap item 13 targets**: `new ProcessStartInfo(...).Environment` is pre-populated with a *copy of the current process's entire environment*, not a blank slate — the previous code only overwrote/added the harness's own explicit keys and never stripped anything inherited, so ambient `CONFORMANCE_*`/`AZURE_*`/`*_PROXY`/`VERBOSE_*` vars from the coordinator's shell, a CI runner, or the corporate NuGet/npm/pip proxy setup could leak straight into the Python child process. Added `Conformance.Harness/InheritedEnvironmentFilter.cs`: a standalone, launcher-agnostic `Apply(ProcessStartInfo)` that strips any inherited key matching `CONFORMANCE_*`/`AZURE_*`/`VERBOSE_*` (prefix) or `*_PROXY` (suffix), case-insensitively, then pins `NO_PROXY`/`no_proxy` to `127.0.0.1,localhost` (the backend under test only ever needs to reach the fakes and itself, both on loopback). Wired into `PythonBackendLauncher.StartAsync` *before* the harness's own explicit env vars are applied, so anything the harness deliberately wants set still wins.
- New pure, process-free unit tests (`InheritedEnvironmentFilterTests.cs`, 3 facts) prove the policy directly against a `ProcessStartInfo` object — no backend or fakes needed, deterministic, ~20ms total: mixed-case `CONFORMANCE_*`/`AZURE_*`/`*_PROXY`/`VERBOSE_*` vars removed while unrelated vars (`PATH`, `USERNAME`) survive; `NO_PROXY`/`no_proxy` always pinned to the loopback-only value even when already set to something else; and the pin still happens with no inherited vars present at all. Mutation-checked: temporarily reduced `Apply` to only the `NO_PROXY` pin (removal logic deleted) → clean, specific `Assert.False` failure (`CONFORMANCE_TEST_HOOKS` still present); restored → green.
- Extended `tests/conformance/README.md` with the full `BackendContract` section promised by item 9's placeholder note: every env var the harness sets and why, the environment-stripping policy and its rationale, the exact `/health` response shape (`{status, version, checks}` — read from `app/backend/app.py`'s `_health_handler`, not guessed), and the shared files (`menuItems.json`, `app/backend/static/index.html`) both the harness and the backend depend on.
- `dotnet test tests/conformance`: 35 passed / 1 skipped / 36 total (3 new tests, no regressions — full Python-backed suite still launches correctly with the new stripping in place). 3× green.
- Commit: `09788ed`.

## 2026-09-24 — feat/conformance-harness Stage B item 14 (#7)

- Renamed `app/backend/test_hooks.py` → `app/backend/conformance_hooks.py` (all four production consumers — `order_state.py`, `session_manager.py`, `rate_limit.py`, `rtmt.py` — updated to `import conformance_hooks`) and `app/backend/tests/test_test_hooks.py` → `app/backend/tests/test_conformance_hooks.py`; updated the one remaining doc-comment reference in `Conformance.Harness/BackendProfile.cs`. Grepped the whole repo for `test_hooks` afterward — zero remaining references.
- Rewrote the module docstring to explicitly separate the two independent, previously-conflated mechanisms: `now(tz)` (business-logic clock — only `order_state.py`'s happy-hour pricing reads it) vs. `seconds(env_var, default)` (asyncio timer durations only). Documented the rate-limit hint-clamping caveat in both `conformance_hooks.py`'s and `rate_limit.py`'s docstrings: `CONFORMANCE_RATE_LIMIT_*_DELAY_SECONDS` only overrides the no-hint default fed into `retry_delay(hint, default, bounds)` — the clamp bounds themselves (`FIRST_RETRY_BOUNDS=(0.5,5.0)`, `SECOND_RETRY_BOUNDS=(2.0,8.0)`) are never overridable, so a scripted rate-limit error carrying a "retry after Ns" hint still clamps into production bounds even under `CONFORMANCE_TEST_HOOKS=1`.
- Found and fixed a real gap: `session_manager.py`'s `_RESUME_SWEEP_INTERVAL_SECONDS` (the idle/grace-expiry background sweep loop's interval, default 15s from `config.yaml`) was never wired through the hooks module at all — any scenario needing that background loop to actually fire would have had to wait the full 15s production interval. Added `CONFORMANCE_SWEEP_INTERVAL_SECONDS`.
- Added `_validate_at_startup()`, called once at module-import time: fail-fast (raises immediately, before any business logic runs) if hooks are enabled and `CONFORMANCE_FIXED_NOW` is set but malformed, instead of only surfacing the error lazily whenever some downstream code first calls `now()`; also logs a `WARNING` via `logging.getLogger("sonic-drive-in")` whenever hooks are active, so an accidentally-enabled deployment is loud in its own logs, not just caught by the guard test below.
- Added `TestNeverInInfraOrDockerfile` (new test class): recursively scans `infra/`, `app/Dockerfile`, and `azure.yaml` for the literal substring `CONFORMANCE_` and fails listing the offending file(s) if found — a standing guard against ever wiring these hooks into a real deployment. Mutation-checked: temporarily appended a `CONFORMANCE_FOO` line to `app/Dockerfile` → clean failure listing `app\Dockerfile`; removed it → green.
- Added `TestStartupValidationFailsFast` (4 tests + 1 warning-log test): proves the naive-timestamp and unparseable-timestamp cases now raise during `importlib.reload()` itself (not lazily at first `now()` call), that a valid fixed-now and hooks-enabled-with-no-fixed-now don't raise, and that enabling hooks logs the expected `WARNING`. Mutation-checked by temporarily neutering `_validate_at_startup()`'s body with an early `return` before the parse/log — 3 of 5 tests failed cleanly (`DID NOT RAISE ValueError` ×2, missing warning ×1), the 2 negative-space tests correctly stayed green; restored → all 24 tests in the file green.
- **Fixed a real, subtle cross-file test-isolation hazard the rename exposed**: `test_test_hooks.py` sorted alphabetically *after* `test_order_state.py`, so its `TestHappyHourIntegration` tests' `importlib.reload(order_state)` (which mints a brand-new `SessionIdentifiers` class object every time) always ran *after* `test_order_state.py` had already collected and run against the original class. Renaming to `test_conformance_hooks.py` moved collection order *before* `test_order_state.py`, so the reload's new class object silently diverged from the one `test_order_state.py`'s `from order_state import SessionIdentifiers` had already captured at collection time — `isinstance()` then failed. Fixed properly (not by re-gaming file-name ordering): added `_isolated_order_state()`, loading a private, throwaway copy of `order_state.py` via `importlib.util.spec_from_file_location` under an alias that's never registered as `sys.modules['order_state']`, so happy-hour re-testing never touches the shared canonical module/class the rest of the suite relies on. `order_state.py` does `import conformance_hooks` (a live module reference, not a bound copy), so the private copy still correctly observes whatever the shared, already-reloaded `conformance_hooks` currently reports.
- `app\backend\tests\test_conformance_hooks.py` + `test_order_state.py` run together in collection order: 71 passed. Full `pytest app/backend/tests -q`: **592 passed, 61 subtests passed** (up from the 586 Stage-A baseline; net +6 from this item's new tests). `ruff check .`: clean. `dotnet test tests/conformance`: 35 passed / 1 skipped / 36 total, confirming the Python backend still starts correctly end-to-end importing the renamed module.
- Commit: `3296d55`.

## 2026-09-24 — feat/conformance-harness Stage B item 15 (#7)

- `CONFORMANCE_BACKEND=dotnet` (the S2 .NET-backend placeholder, issue #7's backend doesn't exist yet) previously always turned into an unconditional skip via `ConformanceBackendNotImplementedException`. Rick's item 15: this must **FAIL** the suite by default, so CI can never silently pass with zero real backend coverage just because the future .NET backend hasn't landed; skipping is now opt-in only, and never honoured in CI.
- Added `Conformance.Harness/CiEnvironment.cs` (`IsCi` — checks `GITHUB_ACTIONS=true` first as the most specific GitHub Actions signal, `CI=true` as a generic fallback) and `Conformance.Harness/DotnetPlaceholderPolicy.cs`: a pure `ShouldSkip(string? conformanceAllowSkip, bool isCi)` function (`!isCi && conformanceAllowSkip?.Trim() == "1"`) plus `BuildException(...)` returning either `ConformanceBackendNotImplementedException` (skip path — same type `ConformanceFixture` already special-cased) or a new, deliberately different `ConformanceBackendUnavailableException` (fail path — nothing catches it specially, so it propagates out of `IAsyncLifetime.InitializeAsync` and fails every test in the collection, exactly like the pre-existing "unknown CONFORMANCE_BACKEND value" arm already did). `BackendLauncherFactory`'s `"dotnet"` switch arm now calls `DotnetPlaceholderPolicy.BuildException(...)` instead of unconditionally constructing the skip exception.
- Extracting the decision as a pure function of two plain inputs (rather than reading `Environment.GetEnvironmentVariable`/`CiEnvironment.IsCi` directly inside branching logic) makes it unit-testable without mutating real, global, parallel-unsafe process environment variables: `DotnetPlaceholderPolicyTests.cs` (6 facts/theories) covers every combination — exact-`"1"` requirement (no truthy-string leniency: `"true"` doesn't count), incidental whitespace tolerance (`" 1 "`), and that CI always wins regardless of the opt-in.
- Verified all three real branches end-to-end (not just the unit tests) by actually running `dotnet test` with `CONFORMANCE_BACKEND=dotnet` set: (1) no opt-in → both smoke tests **FAIL** with the new `ConformanceBackendUnavailableException` message; (2) `+CONFORMANCE_ALLOW_SKIP=1` → both **SKIP**; (3) `+GITHUB_ACTIONS=true` on top of the opt-in → back to **FAIL**, proving CI can't be talked into skipping even if the opt-in env var leaks in from somewhere.
- Added a README section ("Choosing a backend") documenting `CONFORMANCE_BACKEND`/`CONFORMANCE_BACKEND_URL`/`CONFORMANCE_ALLOW_SKIP` and the CI-detection rule.
- `dotnet test tests/conformance`: 45 passed / 1 skipped / 46 total (10 new tests, no regressions). 3× green.
- Commit: `57be769`.

## 2026-09-24 — feat/conformance-harness Stage B item 16 (#7)

- **Found the real gap item 16 targets**: `FakeRealtimeUpstreamServer`/`FakeSearchServer` always bound to `http://127.0.0.1:0`, letting Kestrel pick a random free port every run — fine when the harness itself launches the backend (it just reads the chosen port back and injects it into the child process's environment), but fundamentally broken for `CONFORMANCE_BACKEND_URL` external mode: a backend process already running *outside* the harness's control has no way to discover a randomly-chosen port after the fact. Previously the suite would silently proceed and simply fail to connect (or worse, connect to the wrong thing on a busy dev box), with no explanation of *why*.
- Added an optional `int? fixedPort` parameter (trailing, after the pre-existing `CancellationToken cancellationToken = default`, to avoid breaking the many existing `fake.StartAsync(TestContext.Current.CancellationToken)` positional call sites across the test suite) to both `FakeRealtimeUpstreamServer.StartAsync` and `FakeSearchServer.StartAsync` — binds to `http://127.0.0.1:{fixedPort}` when supplied, `:0` (OS-picked) otherwise.
- Added `Conformance.Harness/ExternalModePortPolicy.cs`: a pure `Resolve(string? backendUrl, string? realtimePortRaw, string? searchPortRaw)` function reading `CONFORMANCE_FAKE_REALTIME_PORT`/`CONFORMANCE_FAKE_SEARCH_PORT`. In external mode (`backendUrl` non-empty) both must be present and valid TCP port numbers (1-65535) or it throws `InvalidOperationException` naming the missing/invalid variable and explaining why; in non-external mode either may be omitted (`null` → OS-picked, safe since the harness controls the launched Python process's env either way) but are still honoured/validated if supplied (useful for a developer who wants deterministic, inspectable ports while still using `CONFORMANCE_BACKEND=python`). Wired into `ConformanceFixture.InitializeAsync`, resolved *before* either fake starts, so a misconfigured external-mode run fails immediately with a clear message rather than partway through fixture setup.
- Pure-function extraction (same pattern as item 15's `DotnetPlaceholderPolicy`) makes this unit-testable without mutating real, global, parallel-unsafe process environment variables: `ExternalModePortPolicyTests.cs` (7 facts/theories) covers non-external-with-nothing-set, non-external-with-explicit-ports-honoured, external-missing-realtime-port, external-missing-search-port, external-with-both-present, invalid port values (`"0"`, `"65536"`, `"not-a-port"`, `"-1"`), and incidental whitespace tolerance.
- Added `FakeFixedPortBindingTests.cs`: a genuine end-to-end proof (starts real Kestrel instances, no mocking) that `StartAsync(fixedPort: ...)` actually binds to the exact requested port for both fakes, plus a regression check that omitting `fixedPort` still lets the OS pick one — this is what makes `ExternalModePortPolicy`'s resolved ports meaningful in practice rather than just validated-and-ignored.
- Verified the real end-to-end failure/success path (not just unit tests) via `dotnet test` with `CONFORMANCE_BACKEND_URL` set: missing `CONFORMANCE_FAKE_REALTIME_PORT` → clean `InvalidOperationException` naming the variable, thrown from `ConformanceFixture.InitializeAsync` before any fake starts; both fixed ports supplied → fixture init succeeds (fails later and separately, as expected, only when the *scenario* itself tries to reach the nonexistent external backend at the placeholder test URL — proving the fakes' own startup was never the problem).
- Documented `CONFORMANCE_FAKE_REALTIME_PORT`/`CONFORMANCE_FAKE_SEARCH_PORT` in the README's "Choosing a backend" section.
- `dotnet test tests/conformance`: 58 passed / 1 skipped / 59 total (13 new tests, no regressions). 3× green.
- Commit: `2590034`.

## 2026-09-24 — feat/conformance-harness Stage B item 17 (#7)

- **Windows Job Object** (`WindowsJobObject.cs`): every launched Python process is now assigned to a job object created with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` right after `Process.Start()`. If this .NET test process is itself killed forcibly (`Stop-Process`, a crash, a CI runner reaping an orphaned job) with zero chance to run any cleanup code, Windows closes every handle the killed process owned — including the job handle — which (because of the kill-on-close limit) makes the OS itself kill the entire Python process tree with no application code involved at kill-time. No-op on non-Windows (P/Invoke guarded behind `OperatingSystem.IsWindows()`, `[SupportedOSPlatform("windows")]` on the interop surface); the CI runner is `ubuntu-latest` and relies on the pre-existing `Process.Kill(entireProcessTree: true)` + the new `AppDomain.ProcessExit` handler instead.
- **`AppDomain.ProcessExit` handler**: cross-platform belt-and-suspenders for the *graceful* exit paths that skip normal `IAsyncDisposable` disposal (an unhandled exception unwinding past it, a stray `Environment.Exit` elsewhere). Registered right after the process starts, unregistered in `ProcessBackend.DisposeAsync` so it never fires twice or outlives the backend instance it guards.
- **Retry on port-bind races** (`PortRaceDetection.cs`, pure function, same extraction pattern as items 15/16's policies): `NetworkUtils.GetFreeTcpPort()` has an inherent TOCTOU race between releasing its probe socket and the backend's own bind. `PythonBackendLauncher.StartAsync` now retries (up to 3 attempts, picking a fresh port each time) only when the process exits within `PortRaceDetection.RaceDetectionWindow` of starting **and** its captured output matches a known bind-failure signature (`errno 98`, `WinError 10048`/`10013`, "address already in use"/"only one usage of each socket address") — any other early exit (a real crash) is never retried, so a genuine bug can't hide behind a slow, flaky-looking eventual pass.
- **Real bug caught while validating the above, fixed before committing**: the race-detection window started at a guessed 5s. A real end-to-end mutation-check (pre-occupying a port with a raw `TcpListener`, then launching the real Python backend against it) showed the actual bind failure surfaces ~4-5s after process start on this box — right at the edge of the window — because `app.py` runs non-fatal reachability probes against the (fake) Azure OpenAI/Search endpoints, each with its own ~2s timeout, *before* attempting its own socket bind. Widened to 20s (comfortably covers the observed latency with margin, still leaves 40s before the 60s health timeout) and re-ran the same real end-to-end probe: it now retries past the occupied port and starts healthy on a fresh one, confirmed via `dotnet test`, not just a mocked unit test.
- **CI**: `.github/workflows/conformance.yml` — added `timeout-minutes` to every job (15m for `python-tests`/`frontend-tests`, 20m for `conformance`) so a genuine hang fails the job instead of burning the whole Actions time budget with no signal; the `dotnet test` step now passes `--blame-hang --blame-hang-timeout 10min --blame-hang-dump-type mini --blame-crash --blame-crash-dump-type mini` (confirmed VSTest, not Microsoft.Testing.Platform, is in play here — `Conformance.Tests.csproj` references `xunit.v3.mtp-off` — so these flags are valid; verified locally that `dotnet test` still runs and passes with them supplied). Dumps land in the already-uploaded `TestResults` artifact on failure.
- Verified YAML validity with `python -c "import yaml; yaml.safe_load(...)"` after every edit.
- New tests: `WindowsJobObjectTests.cs` (2 facts — a real spawned-process-plus-job-object test proving `Dispose()` kills the assigned process, matching the "prove no orphaned python.exe after a killed test run" requirement as a deterministic, automatable proxy for the forced-kill scenario; and a defensive null-return-for-bogus-pid test; both skip cleanly off-Windows) and `PortRaceDetectionTests.cs` (8 facts/theories on the pure heuristic).
- Mutation-checked both: (1) `WindowsJobObject`'s `JobObjectLimitKillOnJobClose` flipped to `0x0` → `Disposing_the_job_object_kills_the_assigned_process` fails cleanly (times out waiting for exit) — restored → green, confirmed no orphaned `python.exe` left behind afterwards (`Get-Process python*` empty); (2) `PortRaceDetection.LooksLikePortBindFailure` neutered to always return `false` → 6 of 10 theory cases fail — restored → green.
- Documented the whole mechanism in `tests/conformance/README.md`'s new "Process lifecycle hardening" section.
- Full suite: 70 passed / 1 skipped / 71 total (10 new tests, no regressions). 3× green. No orphaned `python.exe` processes after the full run (`Get-Process python*` empty).
- Commit: `5c8004d`.

## 2026-09-24 — feat/conformance-harness Linux CI close-race fix (#7)

- Rick reported CI red on Linux only (run 35936172326): `BrowserClientLifecycleTests.Graceful_close_is_answered_by_the_backend_and_observed_via_WaitForCloseAsync` failed `Assert.Equal(NormalClosure, browser.CloseStatus)` with `Actual: null`, passing reliably on Windows. His ask: fix the actual observation race (no sleeps/retries), reproduce on Linux if possible (WSL), audit the rest of the suite for the same class of bug, and add a 50× stress test to shake it out on Windows too.
- **Reproduction**: installed the .NET 11 RC1 Linux SDK tarball per-user under `~/.dotnet11` inside an existing (stopped) Ubuntu WSL distro (public `builds.dotnet.microsoft.com` download, not a package registry — no policy concern) and built the whole solution there against the Windows-mounted repo path — clean build, confirming the fix at least compiles identically on both platforms. A tiny isolated repro against `FakeRealtimeUpstreamServer` (no Python, Kestrel-only) did **not** reproduce a `CloseStatus`-population-timing race on this WSL VM, which redirected the investigation towards the real culprit below rather than a live-property-read timing quirk. Running the real Python backend under WSL was **not** attempted — the existing `.venv` is a Windows venv (incompatible layout on Linux) and creating a parallel Linux Python environment was out of scope for a harness-only fix.
- **Real root cause, found by writing the requested 50-connection stress test and running it against the real Python backend on Windows** (it reproduced the race locally, 1/50, proving this was never Linux-specific): `RealtimeBrowserClient`'s background reader loop was `while (_socket.State == WebSocketState.Open) { receive... }`. `CloseAsync` sends the local close frame via `CloseOutputAsync`, which flips `_socket.State` from `Open` to `CloseSent` on the *caller's* task the moment it completes — concurrently with the reader loop. If that flip landed between two loop iterations (rather than while a receive was already in flight), the while-condition went false and the loop returned **without ever calling `ReceiveAsync` again** — so it never saw the peer's answering Close frame at all, even though `CloseSent` is a perfectly valid, expected state to keep receiving from (that's the entire reason the state exists). Fixed the condition to `while (_socket.State is WebSocketState.Open or WebSocketState.CloseSent)`.
- **Also hardened, per Rick's specific suggestion**: added `WebSocketJson.ReceiveJsonOrCloseAsync` (+ `WebSocketMessageOrClose`) so the reader loop captures `CloseStatus`/`CloseStatusDescription` directly off the Close-type `WebSocketReceiveResult` the instant it's observed, rather than reading `ClientWebSocket.CloseStatus` live off the socket afterward (removes any dependency on when the underlying implementation updates that property, on any platform) — plus a best-effort fallback (`TryCaptureCloseFromSettledSocket`, reads the socket's own `CloseStatus` only once `State` has settled to `Closed`/`CloseReceived`) for the rarer path where the receive that actually observed the close frame surfaces as a `WebSocketException` instead of a clean result. `ReceiveJsonAsync`'s existing "returns null on Close" contract (used by every other caller in the fakes/tests) is preserved unchanged, now implemented in terms of the new method.
- **Audited the rest of the suite** for the same race class (grepped every `.CloseStatus`/`.State ==`/`.State is`/`WebSocketState.` site): this was the *only* `CloseStatus` read anywhere. The handful of `Assert.Equal(WebSocketState.Open, socket.State)`/`Assert.NotEqual(...)` calls elsewhere (`FakeRealtimeScriptingModelTests.cs`) are all immediately after `ConnectAsync` completing or throwing — `WebSocketState` transitions synchronously as part of that call by contract, unlike `CloseStatus`, which depends on the runtime having parsed an incoming frame's payload — so those are not the same bug and were left alone.
- **New test**: `Graceful_close_observes_CloseStatus_reliably_across_50_connections` (`BrowserClientLifecycleTests.cs`) — 50 connect→greeting-round-trip→close→wait→assert cycles against the shared long-lived backend in one `[Fact]`, collecting every failing iteration's index instead of stopping at the first so a second, unrelated flake wouldn't hide behind the first. Ran it standalone 4× back to back after the fix (200/200 close cycles observed `NormalClosure`, 0 failures) — it's what actually reproduced and then confirmed the fix, not just theory.
- Verified: `dotnet test tests/conformance` green 3× (71 passed / 1 skipped / 72 total, no regressions beyond the one new test); solution builds clean under WSL/Ubuntu with the per-user .NET 11 RC1 Linux SDK; pytest 592 passed/61 subtests (untouched — no Python changed); `ruff check .` clean; frontend 116 passed.
- `git status` clean; no `bin/`/`obj/` staged.
- Commit: `caa278e`.

## 2026-09-24 — feat/conformance-harness Stage C item N1 (#7)

- Rick's approve-with-follow-ups review, N1: `RealtimeScript.Rules` was a plain, unsynchronized `List<>` and `QueuedResponses` a plain `Queue<>` — a test arming/clearing a rule mid-scenario could race `FakeRealtimeUpstreamServer.HandleFrameAsync`'s own `foreach` enumeration over `Rules` and throw `InvalidOperationException: "Collection was modified"`.
- `QueuedResponses` is now `ConcurrentQueue<ResponseScript>`; `RespondAsync` uses `TryDequeue` instead of `Count>0`-then-`Dequeue`.
- `Rules` is now a read-only property that snapshots (`ToArray()`) under a private `Lock` on every read; `On()`/new `ClearRules()` mutate the backing list under the same lock. `ClearRules()` replaces the old `Rules.Clear()` call pattern (the one existing call site in `FakeRealtimeScriptingModelTests.cs` updated).
- New test `Rules_can_be_mutated_safely_while_a_concurrent_enumeration_is_in_progress`: a direct `RealtimeScript`-only repro (no socket/Kestrel) — 4 tasks hammering `On()`/`ClearRules()` concurrently with 4 tasks enumerating `Rules`, for 2s.
- Mutation-checked: reverted the `Rules` getter to `return _rules;` (the live list) — the new test failed reliably with `InvalidOperationException: Collection was modified; enumeration operation may not execute.` within ~2s; restored `_rules.ToArray()` — green again.
- Full suite: 72 passed / 1 skipped / 73 total. No regressions.
- Commit: `3e8d10f`.

## 2026-09-24 — feat/conformance-harness Stage C item N2 (#7)

- Rick's N2: `ConnectionRegistry.Add` used to construct **and** publish (signal `WaitForNextAsync` waiters) a connection in one step, called *before* `AcceptWebSocketAsync()` even started — a waiter could see a connection whose socket wasn't attached yet. `FakeRealtimeConnection.SendAsync` silently no-op'd on a null/non-open socket instead of throwing, so this raced quietly (the frame just vanished).
- Split `ConnectionRegistry.Add` into `Create` (construct only) and `Publish` (register + signal). `HandleConnectionAsync` now runs `Create → AcceptWebSocketAsync → AttachSocket → Publish` — by the time any waiter can observe a connection, its socket is guaranteed attached and open. Fixed the stale `ConnectionRegistry.cs:44` doc comment that claimed the old (untrue) ordering.
- `SendAsync` now throws `InvalidOperationException` with a clear message (naming the exact state) instead of silently returning, for both "never attached" and "no longer Open".
- New tests: `Awaited_connection_can_be_sent_on_immediately_with_no_race_100_times` (deliberately doesn't await the client's own `ConnectAsync` before awaiting the connection, to genuinely exercise the race, 100×) and `SendAsync_throws_a_clear_message_once_the_socket_is_no_longer_open`.
- Mutation-checked both halves independently: (1) reverted to the old publish-before-attach order → the 100× test failed at iteration 1 within 30s with "cannot send — the socket has not been attached yet"; restored → green in 2s; (2) reverted `SendAsync` to the old silent no-op → the closed-socket test failed with "No exception was thrown"; restored → green.
- Full suite: 74 passed / 1 skipped / 75 total. No regressions.
- Commit: `4ce9195`.

## 2026-09-24 — feat/conformance-harness Stage C item N3 (#7)

- Rick's N3: rule handlers (and the VAD-like defaults) run fire-and-forget relative to the connection's receive loop; an exception thrown inside one was only ever seen (if at all) by the single `Task.WhenAll` awaited at connection-close time, deep inside Kestrel's own pipeline — no test could ever observe it, so a scenario whose scripted rule silently threw could still look green.
- `FakeRealtimeConnection` now records handler faults in a lock-guarded list: `RecordHandlerFault(Exception)` appends, `DrainHandlerFaults()` snapshots-and-clears (draining, not just reading, so a fault can't leak from one scenario into the next, or vanish silently).
- `ConnectionRegistry.Snapshot()` added — a lock-protected defensive copy of every connection ever accepted (open or closed) — needed to aggregate faults across a scenario's connections.
- `FakeRealtimeUpstreamServer.HandleConnectionAsync`'s `TrackHandler` now wraps every dispatched handler task in `ObserveHandlerFaultsAsync`, which catches and records instead of letting it reach the close-time `Task.WhenAll`. New public `AssertNoHandlerFaults()` drains every connection via the registry snapshot: no-op if empty, rethrows the single fault via `ExceptionDispatchInfo` (preserves the original stack) if exactly one, `AggregateException` for more than one.
- `ConformanceFixture.RunAsync` calls `Realtime.AssertNoHandlerFaults()` right after a successful scenario body, still inside the existing `try` (so it inherits the same backend-diagnostics wrapping on failure) — surfaces a fault even when the scenario's own assertions happened to pass.
- New test `A_throwing_rule_handler_is_surfaced_by_AssertNoHandlerFaults`: scripts a rule that throws on `conversation.item.create` (a real GA event type — an invented `test.*` type would be rejected by the item-9 unknown-event-type check before ever reaching the rule loop), sends that frame, then polls `AssertNoHandlerFaults()` (safe to call repeatedly while empty — it just returns) until the fault appears rather than assuming timing, and asserts the exact exception plus that draining clears it.
- Mutation-checked: made `ObserveHandlerFaultsAsync`'s catch swallow instead of recording → the new test failed by timing out (10s) waiting for a fault that never arrives; restored the recording call → green again.
- Full suite: 75 passed / 1 skipped / 76 total, run 3×. No regressions.
- Commit: `013c233`.

## 2026-09-24 — feat/conformance-harness Stage C item N4 (#7)

- Rick's N4: `BackendProfiles.ShortTimers` shortened every other backend timer but missed `CONFORMANCE_SWEEP_INTERVAL_SECONDS` (`session_manager.py`'s `_RESUME_SWEEP_INTERVAL_SECONDS`, default 15s) — a future resume-sweep scenario would still wait up to 15s per sweep even under `ShortTimers`.
- Added `["CONFORMANCE_SWEEP_INTERVAL_SECONDS"] = "0.2"` to the profile, matching its other ~1s-or-faster overrides; updated the doc comment.
- No scenario in the suite yet exercises session-resume sweeping (extension point for S1.2–S1.4), so this is a config-only passthrough fix with nothing independently mutation-checkable at the harness level — verified by the full suite staying green.
- Full suite: 75 passed / 1 skipped / 76 total. No regressions.
- Commit: `22eae93`.

## 2026-09-24 — feat/conformance-harness Stage C item N5 (#7)

- Rick's N5: three scenarios asserted `Assert.DoesNotContain("Traceback", diagnostics, ...)` directly against raw Python stderr text — the exact kind of language-specific coupling that would break once a C# backend under test exists. The count-based `SuppressNextSessionUpdatedResponse()` mechanism had the same shape of problem: server-wide state that had to be called exactly N times to match N connections, rather than being scoped to the one connection it was meant to affect.
- Added `IBackendUnderTest.UnhandledErrorCount()` to the neutral backend contract. `ProcessBackend` (Python) implements it via a new `CapturedProcessOutput.CountUnhandledErrors()`, a 3-state machine (`Idle`/`AfterErrorHeader`/`InTracebackBody`) over stderr-tagged lines that treats a `logger.exception(...)`-shaped `ERROR: ...` line plus its attached `Traceback (most recent call last):` block as **one** incident, not two (a naive "count ERROR: lines + count Traceback lines" would double-count every real incident). `ExternalBackend` returns 0.
- Replaced all three `Assert.DoesNotContain("Traceback", ...)` sites with the new signal — but the first version asserted it against an absolute zero, exactly like the old design, and a *routine, non-mutated* three-times-green pass caught a genuine unforced failure: the Python backend process is shared across the whole `ConformanceCollection` for speed, so one scenario's incidental backend-side stderr noise (e.g. asyncio teardown warnings from a prior scenario's abrupt disconnect) permanently poisoned every later scenario's "must be zero" check — a real, order-dependent flake, caught in the wild rather than manufactured. Fixed by moving the check into `ConformanceFixture.RunAsync`: it snapshots `UnhandledErrorCount()` as a baseline **before** the scenario body runs and asserts the count is unchanged after, symmetrically with `AssertNoHandlerFaults()` (item N3). The three scenario files no longer assert this themselves.
- Reworked the suppression mechanism: `FakeRealtimeConnection` gained a per-connection `SuppressAllSessionUpdated` flag; `FakeRealtimeUpstreamServer`'s one-shot `SuppressSessionUpdatedOnNextConnection()` is consumed exactly once, right after connection creation and before the socket is accepted, setting the flag on that connection only. `GreetingTimeoutFallbackTests` now calls the new API once instead of the old exact-count API twice.
- New `CapturedProcessOutputTests.cs` unit-tests `CountUnhandledErrors()` against a real short-lived Python process writing only to stderr (avoiding stdout deliberately — the two streams are read by independent async readers into one shared queue with no cross-stream ordering guarantee): one incident for an `ERROR:` line with its attached traceback, zero for ordinary output.
- Mutation-checked: (1) made the `AfterErrorHeader` state double-count a `Traceback` header instead of pairing it with its `ERROR:` line → `CapturedProcessOutputTests` failed (Expected: 2, Actual: 3) → restored → green; (2) made `HandleConnectionAsync` ignore the suppression consume call → `GreetingTimeoutFallbackTests` failed (the fallback-timeout diagnostic text never appeared since `session.updated` was no longer suppressed) → restored → green; (3) the baseline-vs-absolute-zero fix was validated by direct observation of a real flake (above) rather than a synthetic mutation, then confirmed green across 18 additional full-suite runs.
- Full suite: 77 passed / 1 skipped / 78 total, run 3× (plus 18 additional runs while chasing the baseline flake). No regressions.
- Commit: `3a2d26f`.

## 2026-09-24 — feat/conformance-harness Stage C item N6 (#7)

- Rick's N6: `ShortTimersConformanceFixture`/`FixedClockConformanceFixture` had no special handling for external mode (`CONFORMANCE_BACKEND_URL`) — each would resolve the *same* `CONFORMANCE_FAKE_REALTIME_PORT`/`CONFORMANCE_FAKE_SEARCH_PORT` fixed ports as the Default collection and race to bind Kestrel to them concurrently, and even absent that race there's no way for the harness to know whether the one already-running external backend happens to be configured with a non-Default profile's `CONFORMANCE_TEST_HOOKS` overrides.
- Added `ExternalModeProfilePolicy` (pure, unit-tested the same way as `ExternalModePortPolicy`/`DotnetPlaceholderPolicy`): `ShouldSkip(backendUrl, profileName, defaultProfileName)` returns a clear skip reason in external mode when the profile isn't Default, else null. `ConformanceFixture.InitializeAsync` checks this *first* — before resolving any port or starting either fake — and sets the existing `SkipReason` property (already honoured by `RunAsync`), so every test in a non-Default collection skips cleanly with an actionable message instead of racing ports.
- New `ExternalModeProfilePolicyTests.cs` unit-tests the policy directly. Integration-level check: ran `GreetingTimeoutFallbackTests` (ShortTimers) and `HappyHourPricingTests` (FixedClock) with `CONFORMANCE_BACKEND_URL` set to a dummy URL and no fixed ports configured — both skip cleanly rather than erroring.
- Mutation-checked: temporarily forced `SkipReason` to stay `null` (bypassing the policy check) → the same two tests now failed during `InitializeAsync` with `ExternalModePortPolicy`'s "port is not set" exception instead of skipping → restored → clean skip again.
- Documented the policy in `README.md`'s backend-selection section.
- Full suite: 81 passed / 1 skipped / 82 total, run 3×. No regressions.
- Commit: `fb85e53`.

## 2026-09-24 — feat/conformance-harness Stage C item N7 (#7)

- Rick's N7: the fake had no GA `response.cancel` handling, and `response.create` while a response was already streaming was silently allowed to start a second, concurrent response — neither matches GA's documented behaviour.
- `FakeRealtimeConnection` gained per-connection `ActiveResponseId` (the id currently streaming on the default conversation, or null) and `ActiveResponseCancellation` (a `CancellationTokenSource`, cancelled by an accepted `response.cancel`). Both are set **before** `response.created` is sent in `RespondAsync`, so any client that has *observed* `response.created` is guaranteed the server-side state is already active — this removed the need for any explicit test-side synchronisation.
- `response.create` now checks `ActiveResponseId` first: if set, rejects with `conversation_already_has_active_response` instead of starting a second response. New `response.cancel` case dispatches to `HandleResponseCancelAsync`: resolves the optional `response_id`, and if it doesn't target the active response (or nothing is active), rejects with `response_cancel_not_active`, `param: "response_id"` — otherwise cancels the linked token.
- `RespondAsync`'s event loop is now wrapped in try/catch(`OperationCanceledException`)/finally: the paced `Task.Delay` awaits `responseCts.Token` (not the outer `ct`) so an accepted cancel actually interrupts a wait; a per-iteration check also catches cancellation observed between non-paced events. On cancellation, any still-open output item is closed with `status: "incomplete"` (via a new `itemStatus` parameter on `CloseOpenAudioItemAsync`) and a final `response.done` with `status: "cancelled"` and the output accumulated so far is sent — overriding the scripted `DoneEvent`, which is never reached on this path.
- Both new error codes (`conversation_already_has_active_response`, `response_cancel_not_active`) were grepped for verbatim in the full downloaded GA realtime reference and not found; per the item-9 precedent, documented in `README.md`'s "GA validation fidelity" section as plausible-but-**not**-independently-live-verified (no new live probe was run or authorised for this item).
- New `ResponseCancelTests.cs` (3 tests): `Cancel_with_nothing_active_is_rejected_and_the_session_remains_unaffected`, `Cancel_while_streaming_stops_the_response_early_and_reports_cancelled_status`, `Response_create_while_a_response_is_already_active_is_rejected`. The streaming-cancel test needed a short *real* pace (300ms/2s) on its scripted deltas — an unpaced script can race to full completion (including clearing `ActiveResponseId` in the `finally`) before a client's "immediately send a second frame" round-trips back over loopback, even though the client only sends after observing the first frame; this mirrors the existing barge-in `Pace` design intent, not a synchronisation sleep.
- Mutation-checked: (1) forced `targetsActiveResponse = true` unconditionally in `HandleResponseCancelAsync` → `Cancel_with_nothing_active_is_rejected...` failed (10s timeout, no error frame ever arrives) → restored → green; (2) reverted the paced `Task.Delay` to await `ct` instead of `responseCts.Token` → `Cancel_while_streaming_stops_the_response_early...` failed fast (`Expected: "response.output_audio.done", Actual: "response.output_audio.delta"`) → restored → green; (3) bypassed the `ActiveResponseId is not null` guard on `response.create` → `Response_create_while_a_response_is_already_active_is_rejected` failed (`Expected: "error", Actual: "response.created"`) → restored → green.
- Full suite: 84 passed / 1 skipped / 85 total, run 3×. No regressions.
- Commit: `db71637`.

## 2026-09-24 — feat/conformance-harness Stage C item N8 (#7)

- Rick's N8: a batch of documentation/hardening follow-ups. `app/backend/conformance_hooks.py` was already fail-fast for `CONFORMANCE_FIXED_NOW` (item 14) but `seconds()` still silently swallowed any unparseable override and fell back to `default` — the same "lock in a silent ignore" shape item 14 fixed for the clock, just left over for the timer overrides. Also: the `_parse_fixed_now` docstring claimed a trailing "IANA zone" was accepted, which is false — `datetime.fromisoformat` only accepts a numeric UTC offset (or `Z`).
- `seconds(env_var, default)` now raises `ValueError` immediately when hooks are enabled and the override is present but unparseable, NaN, +/-infinity, zero, or negative — absence/empty still falls back to `default` untouched (that's not a misconfiguration). Since every call site (`session_manager.py`, `rate_limit.py`, `rtmt.py`) assigns the result to a module-level constant at its own import time, this makes a bad override a fail-fast, non-zero-exit backend startup failure instead of a silently-wrong timer value for an entire test run — symmetric with the existing `CONFORMANCE_FIXED_NOW` behaviour.
- `test_conformance_hooks.py`: removed `test_seconds_falls_back_on_unparseable_override` (the test that had locked in the old silent-ignore behaviour); added `TestSecondsFailsFastOnInvalidOverride` (6 tests: unparseable/NaN/+-inf/zero/negative must raise with the env var name in the message; unset override and hooks-disabled must not raise).
- `BackendEnvironment.cs`: reclassified `RUNNING_IN_PRODUCTION`, `LOG_LEVEL`, `APP_SESSION_SECRET`, `RATE_LIMIT_RECOVERY_ENABLED` from the "Python-launcher-specific" comment group into the "neutral BackendContract" group — their *names* happen to come from this backend's own config surface, but the underlying need (production-like startup, explicit log level, a session secret, rate-limit recovery enabled) applies to any backend under test. Only `PYTHONUNBUFFERED`/`PYTHONUTF8` (genuine CPython interpreter env vars) remain in the Python-specific group.
- `.github/workflows/conformance.yml`: fixed a stale comment near the `conformance` job claiming `CONFORMANCE_BACKEND=dotnet` "skips cleanly" — item 15 changed that to FAIL by default in CI instead (so CI can never silently report green with zero real backend coverage); the comment now says so. Validated the YAML still parses (`yaml.safe_load`).
- `README.md`: added a full "Test hooks" section (every `CONFORMANCE_*` test-hook var: units, allowed range, consumer, failure behaviour), documented the frozen-clock semantics precisely (RFC 3339 numeric offset only — never an IANA zone name — does not advance, business wall-clock logic only), the rate-limit hint-clamping caveat, and the startup `WARNING` log; updated the env-var table for the `BackendEnvironment` reclassification above and added the previously-undocumented `CONFORMANCE_SWEEP_INTERVAL_SECONDS` row.
- Mutation-checked: reverted `seconds()` to the old catch-and-fall-back body → all 10 relevant cases in `TestSecondsFailsFastOnInvalidOverride` failed with "DID NOT RAISE ValueError" → restored → 36/36 green in `test_conformance_hooks.py`.
- Full suite: 84 passed / 1 skipped / 85 total (.NET), run 3×. `pytest`: 604 passed, 61 subtests passed. `ruff check .`: clean. No regressions.
- Commit: `94f2432`.



## 2026-09-24 — feat/conformance-harness Stage C item N9 (#7)

- Rick's N9: `FakeRealtimeUpstreamServer.RejectNextConnectionWith`/`SuppressSessionUpdatedOnNextConnection` are one-shot switches consumed by the *next* connection accepted — whichever scenario happens to trigger it. A scenario that armed one and never actually opened a new connection afterwards (a failing assertion before the connect, an early return, a copy-paste mistake) used to leave it armed to silently misfire against a completely unrelated later scenario's connection — a confusing failure far from its real cause.
- Added `FakeRealtimeUpstreamServer.AssertNoPendingOneShotSwitches()`: throws a clear `InvalidOperationException` naming which switch leaked if either the handshake-rejection queue is non-empty or the suppression flag is still armed. Wired into `ConformanceFixture.RunAsync` at the very start — before the scenario body runs, so a leaked switch from a *previous* scenario is caught before it can misfire against *this* scenario's connection, matching the existing "assert empty at test start" idiom (`WaitForNoOpenConnectionsAsync`, `AssertNoHandlerFaults`) rather than making each switch individually `IDisposable`: both switches are consumed automatically by ordinary connection traffic in the overwhelming majority of scenarios that use them, so a disposable wrapper would mostly add ceremony around a case that should never legitimately happen.
- Five new self-tests in `FakeRealtimeScriptingModelTests.cs` against a bare (non-fixture) `FakeRealtimeUpstreamServer`: silent when nothing armed; silent once a connection has consumed an armed rejection or an armed suppression; throws naming `RejectNextConnectionWith` when a rejection was armed but never consumed; throws naming `SuppressSessionUpdatedOnNextConnection` when a suppression was armed but never consumed.
- Confirmed no existing caller (`GreetingTimeoutFallbackTests`'s `SuppressSessionUpdatedOnNextConnection`, the scripting-model tests' `RejectNextConnectionWith`) is affected: all arm their switch *inside* the `RunAsync` body (after the new pre-check runs) or use a bare, non-fixture server instance that never goes through `RunAsync` at all.
- Mutation-checked: disabled the guard body (via a non-constant-foldable `if (Guid.NewGuid() != Guid.Empty) return;` to dodge CS0162 under warnings-as-errors, same trick as item N7's mutation) → both new throw-tests failed with "Assert.Throws() Failure: No exception was thrown" → reverted → green.
- Full suite: 89 passed / 1 skipped / 90 total, run 3×. `pytest`: 604 passed, 61 subtests (unchanged — this item is C#-only). `ruff check .`: clean. No regressions.
- Commit: `9145990`.

## 2026-09-24 — feat/conformance-harness N3 fix (#7)

- Rick's final PR review (approve-with-follow-ups) named one blocker, N3: a browser dropping the connection mid-stream (a normal `await using` teardown, not a bug) could still be misrecorded as a "handler fault" by `ObserveHandlerFaultsAsync`, and the fault-assertion ordering meant it could surface against the *wrong*, later scenario instead of the one that actually caused it.
- Implemented Rick's five numbered items exactly: (1) `FakeRealtimeConnection.SendAsync` now throws a dedicated `FakeConnectionClosedException` instead of a generic `InvalidOperationException` when the socket is no longer open. (2) `ObserveHandlerFaultsAsync` no longer records `FakeConnectionClosedException`, nor `OperationCanceledException` once either of the connection's two teardown signals has fired. (3) `ConformanceFixture.RunAsync` now awaits `Realtime.WaitForNoOpenConnectionsAsync(30s)` after the scenario body returns — failing with a clear message naming any still-open connection ids — **before** asserting handler faults. (4) Fault assertion is scoped to connections accepted at/after a watermark (`ConnectionRegistry.TotalAcceptedCount`/`SnapshotSince`) captured at the start of each scenario. (5) New regression test.
- Two additional root causes surfaced while proving the fix with the new regression test, beyond Rick's literal spec:
  - **Classification-guard gap**: `ObserveHandlerFaultsAsync`'s exclusion originally checked only `connection.TeardownCancellation.IsCancellationRequested` — but a harder, transport-level abort (a browser's `ClientWebSocket` disposed after only `CloseOutputAsync`, never a full graceful `CloseAsync`) cancels Kestrel's own `HttpContext.RequestAborted` (`ct`) on a separate continuation from whichever code relays that into `TeardownCancellation`; two independently-registered callbacks on the same `CancellationTokenSource` have no documented ordering guarantee, so the two could be observed out of order under load. Fixed by checking `ct.IsCancellationRequested` **directly** (a synchronous field read, no ordering dependency) alongside `TeardownCancellation` in `ObserveHandlerFaultsAsync`'s guard, `RespondAsync`'s own catch guard, and its cancelled-response softlanding guard. Also added a `ct.Register(...)` callback in `HandleConnectionAsync` so `TeardownCancellation` is reliably cancelled the instant `ct` fires too, as a second, independent layer of defence.
  - **Ordering gap in `HandleConnectionAsync`**: outstanding per-frame handler tasks were only awaited on the *graceful* receive-loop exit (`break`); an exceptional exit (e.g. `ReceiveJsonAsync` throwing because `ct` fired on a hard abort) skipped straight to `finally`, which only cancelled `TeardownCancellation` and called `NotifyClosed` — meaning `WaitForNoOpenConnectionsAsync` could see the connection as "closed" before an in-flight handler had actually finished or had its exception classified. Restructured so awaiting outstanding handler tasks happens unconditionally in `finally`, on every exit path, strictly before `NotifyClosed`.
- New `HandlerFaultTeardownRegressionTests.cs`: scenario A opens a raw connection directly against the fake, scripts a paced 2-delta response, sends `response.create`, then drops the socket mid-stream (`CloseOutputAsync` + immediate `Dispose`, triggering `ClientWebSocket`'s internal `Abort` — the harder of the two teardown shapes); an empty scenario B runs immediately after. The A→B pair loops 20×.
- Mutation-checked both items per Rick's spec:
  - Revert item 2 (drop the exclusion clauses): regression test fails deterministically on the first iteration — `System.InvalidOperationException : A task was canceled.` (a `TaskCanceledException` from `RespondAsync`'s paced delay) surfaces all the way through `AssertNoHandlerFaults` — exactly the fault this fix eliminates. Restored → green.
  - Revert item 3 (skip the pre-check): with the direct-`ct`-check classification fix in place, this alone no longer reproduces a failure — the direct check closes the race at its root regardless of assertion timing (confirmed clean across 5 isolated + 4 full-suite runs). To validate item 3's own value as Rick intended, combined it with reverting to his literal, narrower `TeardownCancellation`-only guard *and* removing the `ct.Register` relay: that three-way combination reproduced the intermittent failure exactly as expected — **4 of 8 full-suite runs failed**. Restored the fully-fixed code and re-verified 8/8 clean afterward.
- Audited the browser client (`RealtimeBrowserClient`) for the same bug class per the coordinator's request: **no fix needed**. Each instance is scoped to a single test's `await using` block with no shared, cross-scenario fault list — an unusual unswallowed reader-loop exception (e.g. on a backend-initiated 4000/4002 close) could only ever fail that same test, never leak into an unrelated later one. `PumpReceivedFramesAsync`/`ReceiveJsonOrCloseAsync` already treat any close code as clean, non-exceptional.
- Full suite: 90 passed / 1 skipped / 91 total, run **8×** (exceeds the 3× bar, extra runs taken given the intermittent race found). The 50-connection close-stress test (`BrowserClientLifecycleTests.Graceful_close_observes_CloseStatus_reliably_across_50_connections`) included and green in every run. `pytest`: 604 passed, 61 subtests (unchanged — N3 is C#-only). `ruff check .`: clean. `npm test` (frontend): 116 passed. `git status`: clean.
- Commit: `0e39923`.

