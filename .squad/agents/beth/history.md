# Beth — History

## 2026-09-23 — Joined the Sonic Squad

- Added by the coordinator for the C# / .NET 11 port (issue #4), with Brian's approval of the direction: keep the Python backend, add a feature-equivalent C# backend on .NET 11 RC 1, validate both with one shared conformance suite, deploy them side by side, and later unify the Sonic, McDonald's and Dunkin personas in both languages.
- Toolchain verified: SDK `11.0.100-rc.1.26425.128`, installed per-user at `%LOCALAPPDATA%\Microsoft\dotnet11` (not on PATH, so call it by full path). NuGet restores only through `packagefeedproxy.microsoft.io`.
- Key design rule inherited from the analysis: Python's asyncio gives each session single-threaded semantics that the code relies on implicitly. The C# port preserves them with one sequential event loop per session (a `Channel<T>` consumed by one loop), not locks.

## 2026-09-23 — feat/conformance-harness (#7, #11)

- Implemented `tests/conformance/` (Central Package Management via `Directory.Packages.props`, restoring only through `packagefeedproxy.microsoft.io`) with `Nullable=enable` + warnings-as-errors in every suite project.
- Confirmed xUnit v3 APIs assumed during scaffolding actually exist and work on `xunit.v3` against SDK `11.0.100-rc.1.26425.128`: `Assert.Skip(string)`, `Xunit.TestContext.Current.CancellationToken`, `IAsyncLifetime` with `ValueTask`.
- Found and fixed a WebSocket server-side close-handshake bug in `FakeRealtimeUpstreamServer`: `socket.State` is `CloseReceived` (not `Open`) right after receiving a client close frame, so a naive `state == Open` guard skipped completing the handshake and the client's `CloseAsync()` threw. Fixed by accepting `Open or CloseReceived` and catching `WebSocketException` best-effort.
- Fixed `HealthEndpointTests` to parse JSON instead of raw-substring-matching `"status":"healthy"` (the real payload has spaces).
- Cross-platform fix for CI (#11): `RepoPaths.PythonExecutable()` was hardcoded to `.venv/Scripts/python.exe`; branched on `OperatingSystem.IsWindows()` since GitHub-hosted Linux runners use `.venv/bin/python`. No regression: 8/8 green ×3 on Windows after the change.
- `dotnet test tests/conformance` green 3× in a row at multiple checkpoints; `.gitignore` correctly excludes `bin/`/`obj/`.
- Commits: `817f487` (harness + smoke scenario), `9f8188a` (cross-platform fix).
- Decision logged: `.squad/decisions/inbox/beth-dotnet-harness-implementation.md`.

## 2026-09-24 — feat/conformance-harness Stage A (Rick's PR #22 review, #7/#11)

- Implemented Rick's "must fix before merge" items 1–7 + 19, one commit per item (items 2+3 share one engine commit with an explicit rationale, since both touch the same `RespondAsync` method and item 3 subsumes item 2's requirement):
  - **1** (`1e33e5f`): CI now runs `npm ci && npm run build` in `app/frontend` before pytest and `dotnet test`; the harness itself now fails fast with a clear message (`'...\static\index.html' does not exist...`) instead of an opaque "backend exited early" when the frontend hasn't been built.
  - **7** (`ee9fc91`): `FindRepoRoot` now works in git worktrees — accepts `.git` as either a directory or a file (worktree pointer), falls back to marker files.
  - **19** (`7d134d1`): moved `global.json` to the repo root pinning `11.0.100-rc.1.26425.128` with `rollForward: latestPatch`; removed the duplicate under `tests/conformance`.
  - **6** (`89a3c8c`): the deflate scenario now sets `CollectHttpResponseDetails = true` so it can actually observe (and fail on) negotiated compression; mutation-checked by temporarily forcing `ws_compression: true` in the Python config — the test failed as expected, reverted, green again.
  - **5** (`0371029`): replaced the shared server-level frame log in `FakeRealtimeUpstreamServer` with per-connection `FakeRealtimeConnection` (id, own frame log, api-key, model query param) tracked by a new `ConnectionRegistry` exposing `WaitForNextConnectionAsync`/`WaitForNoOpenConnectionsAsync`; rewrote `SmokeSessionBootstrapTests` onto the new API.
  - **4** (`28e56d3`): `RealtimeSessionState.AssistantAudioSeen` is now set the moment the fake emits the first `response.output_audio.delta`; added a self-test proving `cannot_update_voice` actually fires once audio has been sent (was previously wired in the validator but never exercised by a scripted end-to-end response).
  - **2+3 engine** (`3de354e`): rewrote `RespondAsync` to emit the full GA response item lifecycle — `response.output_item.added` → `conversation.item.added` → `response.content_part.added` → (audio deltas | function-call-arguments.done) → `response.content_part.done` → `response.output_item.done` → final `response.done` carrying accumulated `output[]` and a new `BuildUsage()` GA-shaped `usage` object; every event now carries `event_id`, item-scoped events carry `item_id`/`output_index`/`content_index`. Added `RealtimeSessionState.LastConversationItemId` for `previous_item_id` chaining.
  - **2 scenario** (`688d713`): new `ResponseDoneRoundTripTests` proves the browser receives `extension.round_trip_token` and the backend logs no traceback past the greeting. Mutation-check reproduced the real `rtmt.py` bug this guards against: `response.done` without `output`/`usage` throws `KeyError: 'output'` inside `_process_message_to_client`, caught by `_forward_messages`'s outer handler, which silently kills both directions of the WebSocket forwarding — exact traceback captured and pasted into the commit message.
  - **3 scenario** (`bfdce90`): new `UpdateOrderToolCallTests` proves a scripted `update_order` function call round-trips correctly: `function_call_output` reaches the fake upstream for the right `call_id`, and `extension.middle_tier_tool_response` reaches the browser client.
- Investigated `rtmt.py`/`tools.py` read-only to ground items 2/3/4 in the real GA contract: confirmed the unconditional `message["response"]["output"]` at line ~874 is the exact root cause item 2 targets; confirmed `update_order`'s argument schema (`action`, `item_name`, `size`, `quantity`, optional `price`) and that a raw client `response.create` passes through `_process_message_to_server` unchanged (only `session.update` is specially handled), enabling deterministic test-driven second turns without simulating audio/VAD timing.
- Full clean-state re-validation after restoring `app/backend/static` (moved aside earlier to prove item 1's fail-fast message): `dotnet test tests/conformance` 3× green (14/14 each, ~1s), `pytest app/backend/tests -q` 586 passed/61 subtests, `ruff check .` clean, `npm test` 116 passed, `git status` clean, no `bin/`/`obj/` tracked.
- Extension points left for Stage B: per-connection scripting (item 8) — `Script`/`AutoRespond`/`QueuedResponses` are still server-level by design, safe today only because xUnit's collection fixture serializes tests in `ConformanceCollection`; `usage` values in `BuildUsage()` are plausible but not GA-exact since no test currently asserts precise token counts.
- Decision logged: `.squad/decisions/inbox/beth-stage-a-ga-lifecycle.md`.

## 2026-09-24 — feat/conformance-harness Stage B item 8 (#7)

- Moved the scripting surface from server-level to per-connection (`063f7cb`): `FakeRealtimeConnection` now owns `Script` (a fresh `RealtimeScript.WithVadDefaults()` per connection), `SessionState`, a send lock (`SendAsync` behind a `SemaphoreSlim`), `CloseAsync`/`Abort`, `TimeProvider`, and `NewEventId()`. Two-phase construction — `ConnectionRegistry.Add(...)` runs *before* `AcceptWebSocketAsync`, `AttachSocket(socket)` runs after — preserves the pre-existing guarantee that a test's own `ConnectAsync` returning means the connection is already visible to `WaitForNextConnectionAsync`; got this backwards once (registering after accept) and hit a real race that hung a test, fixed by restoring the original ordering.
- `RealtimeScript` gained rule-based triggers: `On(predicate, handler)` plus a `WithVadDefaults()` factory registering two default rules — `input_audio_buffer.append` fires the `speech_started → speech_stopped → committed → transcription.completed` sequence immediately (VAD-*like*, not VAD-*faithful*: no real silence delay, since simulating it buys nothing black-box and adds non-determinism), and `conversation.item.create` acks with `conversation.item.added`. Deleted the dead `RawErrorEvent` record; added `AudioDeltaEvent.Pace` for barge-in pacing.
- `FakeRealtimeUpstreamServer`: added `RejectNextConnectionWith(statusCode)` — a FIFO one-shot queue checked before `AcceptWebSocketAsync`, so a queued rejection fails the handshake with a raw HTTP status instead of completing the 101 upgrade (necessarily server-level: no connection object exists yet). Rewrote the receive loop to be non-blocking: each frame is dispatched to a fire-and-forget task tracked in an `outstanding` list (drained via `Task.WhenAll` once the socket loop exits), so a still-streaming `response.create` and new incoming frames are handled concurrently, serialized only at the socket-write level by `SendAsync`'s semaphore.
- New tests (`FakeRealtimeScriptingModelTests.cs`, 6 total incl. a 2-case theory): handshake rejection (401/429) asserting `ClientWebSocket.HttpStatusCode` via `Options.CollectHttpResponseDetails = true`; one-shot (not sticky) rejection; VAD-default ack ordering; `conversation.item.create` ack; per-connection isolation (connection A's cleared rules / queued failure must not leak onto fresh connection B).
- While mutation-checking, found frame waits in the codebase rely solely on `TestContext.Current.CancellationToken` with no bounded timeout on `WebSocketJson.ReceiveJsonAsync` — a real regression hangs the test host instead of failing (confirmed: had to use `--blame-hang-timeout` to unstick a `dotnet test` invocation). Added a local `ReceiveJsonWithTimeoutAsync` helper (10s linked `CancellationTokenSource`) in the new test file so regressions there fail cleanly; flagging this as a gap the shared `WebSocketJson` helper should probably close in a later item, since every existing test file has the same exposure.
- Mutation-checked: disabled the VAD rule's predicate → the VAD-sequence test fails with a clean `TimeoutException` in 10s (restored → green); made `RejectNextConnectionWith` a no-op → all 3 rejection tests fail with "no exception was thrown" (restored → green).
- `dotnet test tests/conformance`: 20/20 passing, run 3× green (one earlier red run in the same shell session was a one-off flake from back-to-back filtered invocations, not reproducible in 3 consecutive full runs).
- Commit: `063f7cb`.

## 2026-09-24 — feat/conformance-harness Stage B item 9 (#7)

- Re-derived the GA `session.update`/client-event validation surface independently of `app/backend/rtmt.py` (only read `rtmt.py` afterwards, as a cross-check, never as the source): fetched the official OpenAI GA realtime API reference (`https://developers.openai.com/api/reference/resources/realtime`, 2026-09-24) into a local scratch copy and grepped it for the `Realtime Session Create Request`, `Session Update Event`, `Realtime Error`/`Realtime Error Event`, and `Realtime Client Event` sections. Confirmed the existing 14 top-level `session` keys independently (reassuring, not coincidental) and pulled the `audio.input`/`audio.output` nested-shape allow-lists and an 11-member client-event-type union straight from the doc.
- Ran **one live probe** against the real Azure OpenAI GA realtime endpoint (subscription `44847a42-6b69-4e6c-b7e5-ce7140469dd6`, `wss://cog-axgpampkq3yfa.openai.azure.com/openai/v1/realtime?model=gpt-realtime-2.1`, bearer via `az account get-access-token`), per the coordinator's explicit one-probe authorization, and recorded four findings (full detail + honest "not independently verified" callouts in `tests/conformance/README.md`):
  1. Unknown top-level session key → `error.code = "unknown_parameter"` (not the coarse `error.type`) — **this caught a real bug**: the fake was using `"invalid_request_error"` as the specific code. Fixed.
  2. Missing `session.type` → `error.code = "missing_required_parameter"`, `param = "session.type"`.
  3. Unknown top-level client event type → `error.code = "invalid_value"`, message enumerating the service's own exact 12-member supported-type list — which includes `session.close`/`transcription_session.update` (not obvious from doc prose alone) and omits `output_audio_buffer.clear` (which the doc does list). Kept the union of both (13 entries) so nothing either source promises is wrongly rejected.
  4. Missing `Authorization` header → plain HTTP 401 at the handshake, no JSON frame.
  Did **not** attempt to live-verify `cannot_update_voice` or reasoning-on-1.5 codes (would need session/model state unreachable in one scripted probe) — said so explicitly in code comments and the README instead of guessing, per instruction.
- `GaSessionValidator.cs`: rewrote the class doc with full citations; added `AudioInputKeys`/`AudioOutputKeys` and nested-key rejection (new); added `GaClientEventTypes` + `ValidateClientEventType()` (new); fixed the `unknown_parameter` code bug above; added a `session.type`-required check, ordered *last* (after unknown-key/reasoning/cannot_update_voice) so it only fires when nothing more specific already rejected the frame — required because several existing hand-crafted test payloads omit `type` and rely on an earlier rule firing, and because real Python-driven E2E traffic is unaffected (`rtmt.py`'s `_to_ga_session()` always sets `type: "realtime"`, confirmed by reading, not editing, the file).
- `FakeRealtimeUpstreamServer.cs`: dispatch now rejects unrecognised top-level client event types before any rule/switch runs (extracted a shared `SendValidationErrorAsync` helper so `session.update` and this new check share one error-JSON code path). Added opt-in `RequireApiKey`/`ExpectedApiKey` (default off — zero existing raw-connect tests set an `api-key` header, so default-on would have broken ~15 of them) with a 401 handshake rejection when enabled; wired on only for `ConformanceFixture`'s shared Python-backed connection (safe: `rtmt.py` always sends the correct key).
- `BackendEnvironment.cs`: extracted the two hardcoded key literals into named `OpenAiApiKey`/`SearchApiKey` constants so the fixture and the environment builder can't drift.
- 9 new tests in `FakeRealtimeScriptingModelTests.cs` (missing `session.type`; unknown top-level key — added specifically to guard the bug fix, since nothing previously asserted that code; nested `audio.input`/`audio.output` unknown-key rejection ×2 theory cases; unknown client-event-type rejection; 401 missing/wrong api-key ×2; 200 correct-key accepted); fixed two pre-existing hand-crafted payloads in `FakeRealtimeScriptingTests.cs` that needed `session.type` added to keep passing under the new required-type rule.
- Mutation-checked every new rule by disabling it in isolation and confirming a clean, specific failure, then restoring to green: `session.type` check (assertion mismatch), nested audio-key check (both theory cases threw `KeyNotFoundException` — no error frame arrived), client-event-type check (bounded 10s timeout — dispatch silently proceeded), api-key check (both 401 tests got "no exception was thrown"), and the `unknown_parameter` code fix itself (reverting it to the old `"invalid_request_error"` value failed the new guard test with a clean string-mismatch).
- Wrote `tests/conformance/README.md`'s "GA validation fidelity — live-probe evidence" section (doc + probe citations, the four recorded findings, and the two explicit non-verified callouts) — this is the first content in that file; a later item will add the full `BackendContract` table.
- Cleaned up all research scratch files (`_ga_realtime_ref.md`, `_live_probe{,2,3}.py`) before commit — none contained secrets (bearer token only ever lived in an environment variable) but none belonged in the repo either.
- `dotnet test tests/conformance`: 28/28 passing, 3× green.
- Commit: `0afdc7e`.

## 2026-09-24 — feat/conformance-harness Stage B item 10 (#7)

- GA `session.update` is a partial patch, not a replacement, so `session.updated` must echo the full *accumulated* effective session across every prior update on that connection, not just the latest delta. Added `RealtimeSessionState.EffectiveSession` (a mutable `JsonObject`) and `MergeSessionUpdate(JsonElement)` implementing that merge: top-level keys overwrite; `audio.input`/`audio.output` merge one level deeper so setting `audio.input.format` in a second update doesn't drop `audio.output.voice` set by a first. `FakeRealtimeUpstreamServer.HandleSessionUpdateAsync` now calls the merge after validation passes and builds the `session.updated` response from an independent clone of `EffectiveSession` (round-tripped through `JsonNode.Parse(...ToJsonString())` so the transient server-assigned `id`/`object`/`model` overlay never mutates the stored state) instead of the previous stub `{id, object, model}`-only echo.
- New test `Session_updated_echoes_the_full_accumulated_effective_session_across_updates` (`FakeRealtimeScriptingModelTests.cs`): sends two sequential `session.update` frames (first sets `instructions`/`tool_choice`/`tools`/`audio.output.voice`; second only touches `audio.input.format`) and asserts the final `session.updated` still contains everything from both. Mutation-checked: guarded the `MergeSessionUpdate` call off → clean `KeyNotFoundException` (instructions missing from the second echo); restored → green.
- **Real Python finding** (not fixed here, per the coordinator's explicit instruction): read `app/backend/rtmt.py`'s `_process_message_to_client` (read-only) and confirmed it scrubs `instructions`/`tools`/`tool_choice`/`max_response_output_tokens` from `session.created` before forwarding to the browser (comment: "Hide the instructions, tools and max tokens from clients") but has **no equivalent case for `session.updated`**, and `session.updated` is also absent from `audio_pipeline.py`'s `_PASSTHROUGH_SERVER_TYPES` — so it falls through untouched and the full system prompt + tool schema are forwarded verbatim to the browser on every `session.updated` frame. Confirmed empirically, not just by code reading: wrote `SessionUpdatedClientVisibilityTests.Browser_never_receives_instructions_or_tools_in_session_updated` against the real Python backend via `ConformanceFixture`, ran it **without** `Skip` first, and it failed cleanly: `System.InvalidOperationException : The browser must never receive the system prompt via session.updated.` Restored `[Fact(Skip = "...")]` with a reason citing the exact rtmt.py behavior and this empirical result, referencing the follow-up issue the coordinator will file. **Flagging prominently for the coordinator: this is a real backend bug, not a harness gap — session.updated leaks the full system prompt and tool schema to any browser client.**
- `dotnet test tests/conformance`: 29 passed / 1 skipped / 30 total. Suite run 5× in a row green after this change; one earlier isolated failure immediately following a filtered single-test invocation was a port-release race between the filtered run's backend process and the next full run's — no orphaned `python.exe`/`dotnet` processes found afterwards, and 5 consecutive clean full runs since. Flagging as motivation for Stage B item 17 (process lifecycle), not yet implemented at this point in the branch.
- Commit: `9bd552f`.

## 2026-09-24 — feat/conformance-harness Stage B item 11 (#7)

- `RealtimeBrowserClient` hardened per item 11: added an `Origin` request header on the handshake (real browsers always send one, even same-origin; defaults to the backend's own HTTP origin since app/backend/static serves the built frontend same-origin in production — a real dev-server origin doesn't apply here). Added `CloseStatus`/`CloseStatusDescription` properties and a public `CloseAsync(status, description, ct)` for test-initiated graceful closes, plus `WaitForCloseAsync(timeout, ct)` — a `TaskCompletionSource` signaled from the reader loop's `finally` block, awaited with a bounded timeout instead of a sleep, so tests can assert on the close deterministically.
- Found and fixed a real concurrency bug while wiring this up: `ClientWebSocket.CloseAsync` performs its own internal receive loop while waiting for the server's answering Close frame, which races with the background reader loop's already-in-flight `ReceiveAsync` and throws "there is already one outstanding read call for this WebSocket instance." Switched both the new `CloseAsync` and `DisposeAsync` to `CloseOutputAsync` (send-only — no competing receive) and let the existing reader loop observe the server's answering Close frame naturally (it already treats a Close message as a null frame and exits, now also signalling `WaitForCloseAsync`). Also fixed `DisposeAsync` to drain the reader task before touching the socket at all, closing the same race on the teardown path (previously it called `CloseAsync` immediately after cancelling the reader, with no guarantee the in-flight receive had actually unblocked yet).
- Added typed send helpers matching every message type `app/frontend/src/hooks/useRealtime.tsx` actually sends (`input_audio_buffer.append`/`.clear`, `response.cancel`, `extension.set_verbose_logging`/`set_log_to_file`/`set_voice`/`end_session`/`resume`), plus `SendResponseCreateAsync()` for the one test-only pattern (`UpdateOrderToolCallTests`) that previously built a raw `JsonObject` inline.
- New test `BrowserClientLifecycleTests.Graceful_close_is_answered_by_the_backend_and_observed_via_WaitForCloseAsync`: connects, completes the greeting round trip, calls `CloseAsync`, awaits `WaitForCloseAsync`, and asserts the backend answered with `NormalClosure` — confirmed this is aiohttp's own `autoclose` protocol behavior (`rtmt.py`'s `from_client_to_server` loop just `break`s on `WSMsgType.CLOSE`, sends nothing itself). Mutation-checked: disabled the reader loop's close signal behind a non-constant guard → clean `TimeoutException` ("Socket did not close within 00:00:30") after the full 30s bound; restored → green.
- `dotnet test tests/conformance`: 30 passed / 1 skipped / 31 total, 3× green.
- Commit: `2f49af9`.

## 2026-09-24 — feat/conformance-harness Stage B item 12 (#7)

- `CONFORMANCE_TEST_HOOKS` and its overrides are read once at Python module-import time (e.g. `rtmt.py`'s `_SESSION_CONFIGURED_TIMEOUT_SEC = test_hooks.seconds(...)` is a module-level constant), so a running backend process can never switch profiles mid-suite — each profile needs its own dedicated Python process, hence its own xUnit collection.
- Added `BackendProfile`/`BackendProfiles` (`Conformance.Harness/BackendProfile.cs`): a named `ExtraEnvironment` dictionary layered onto the launcher. `Default` (hooks off — most of the suite), `ShortTimers` (hooks on, every currently-wired timer override collapsed to ~1s: idle/grace/nudge/greeting-timeout/rate-limit retries), `FixedClock(instant)` (hooks on, `CONFORMANCE_FIXED_NOW` set to an ISO-8601 instant with explicit UTC offset, timers left at production defaults).
- `ConformanceFixture` gained a `protected virtual BackendProfile Profile => BackendProfiles.Default;`, wired into `BackendLauncherFactory.StartAsync(..., extraEnvironment: Profile.ExtraEnvironment)`. New `ShortTimersConformanceFixture`/`ShortTimersConformanceCollection` and `FixedClockConformanceFixture`/`FixedClockConformanceCollection` (`BackendProfileFixtures.cs`) each override `Profile` and get their own Python process, following the existing `ConformanceFixture`/`ConformanceCollection` pattern exactly.
- `FixedClockConformanceFixture.Instant` is hardcoded to `2026-07-04T15:00:00-05:00` (America/Chicago, CDT) — inside `config.yaml`'s `happy_hour_start: 14`/`happy_hour_end: 16` window (store-local, matching `order_state.py`'s default `STORE_TIMEZONE`) — so the happy-hour proof test is deterministic regardless of what day or hour the suite actually runs on.
- Added `FakeRealtimeUpstreamServer.SuppressNextSessionUpdatedResponse()`: a one-shot, server-level FIFO counter (same pattern as the existing `RejectNextConnectionWith`), consumed atomically inside `HandleSessionUpdateAsync` *after* the session state is still validated/merged as normal — only the reply frame is swallowed. Armed before any connection exists, so there's no race with `WaitForNextConnectionAsync`: the suppression is consumed synchronously by whichever `session.update` the fake processes next, and nothing else could reach the fake first.
- **`GreetingTimeoutFallbackTests`** (ShortTimers collection): proves `CONFORMANCE_GREETING_TIMEOUT_SECONDS` actually shortens `rtmt.py`'s `send_greeting_once` fallback wait. Discovered while writing this that `session_configured` (rtmt.py) is set by *either* the bootstrap `session.update`'s reply or the browser's forwarded `session.update`'s reply — suppressing only the first still let the second's near-instant reply set it before any timeout could fire, so the greeting fired via the normal trigger instead of the fallback. Fixed by arming the suppression twice (once per reply). Asserts the greeting `response.create` still arrives within 3s despite both replies being suppressed — comfortably above the profile's ~1s override, comfortably below the 5s production default — and that the backend logged its own `"No session.updated within"` warning, so this can only pass via the fallback path, not by accident. Mutation-checked: temporarily set the profile's override to 10s (larger than the 3s test bound) — clean `InvalidOperationException` (timeout) with the honest inner backend log showing the greeting never fired within the bound; restored to `1` → green.
- **`HappyHourPricingTests`** (FixedClock collection): scripts an `update_order` "add" of `"Cherry Limeade"` (a name that doesn't need to exist in `menuItems.json` — `menu_utils.infer_category`'s keyword fallback matches `"limeade"` → `"drinks"`, which `order_state.py`'s happy-hour discount applies to) at `price=4.00`, and asserts the `extension.middle_tier_tool_response`'s `tool_result` (a JSON-serialised `OrderSummary`, per `tools.py`'s `client_text=json_order_summary`) has `finalTotal` == `4.00 * 0.5 * 1.08` == `2.16` (50% happy-hour discount, then 8% tax, both from `config.yaml`'s `business_rules`). Mutation-checked: temporarily moved `FixedClockConformanceFixture.Instant` to `10:00` (outside the 14:00–16:00 window) — clean `Assert.Equal` failure, actual value `4.32` (the exact undiscounted total), proving the assertion really depends on the frozen clock; restored to `15:00` → green.
- `dotnet test tests/conformance`: 32 passed / 1 skipped / 33 total (two new collections, each its own Python process — measured no meaningful runtime increase, still ~2s total). 3× green.
- Commit: `876bc83`.

## 2026-09-24 — feat/conformance-harness Stage B item 13 (#7)

- Split the previously-single `PythonBackendOptions` into a truly **neutral `BackendContract`** record (`Conformance.Harness/BackendContract.cs`: `RealtimeBaseUri`/`SearchBaseUri`/`Port`/`Deployment`/`Voice`/`SearchIndex`/`StoreTimeZone`, plus `const`s for the fixed API keys and every Azure AI Search field/config name) representing the facts *any* backend implementation needs to satisfy — Python today, the future .NET backend once S2 exists (issue #7) — versus a slimmed-down `PythonBackendOptions` (now just `ExtraEnvironment`) holding only this-launcher's own extras. `BackendEnvironment.Build(BackendContract, PythonBackendOptions)` now clearly comments which vars come from the neutral contract vs. which are Python-launcher-specific quirks (`RUNNING_IN_PRODUCTION`, `LOG_LEVEL`, `PYTHONUNBUFFERED`, `PYTHONUTF8`, `APP_SESSION_SECRET`, `RATE_LIMIT_RECOVERY_ENABLED`).
- `BackendLauncherFactory.StartAsync` kept its existing loose-parameter public signature (single call site: `ConformanceFixture.cs`) but now builds a `BackendContract` internally via `BackendContract.ForPort(...)` before dispatching to `PythonBackendLauncher.StartAsync(contract, options, ct)` — a future `DotnetBackendLauncher` would take the identical `BackendContract` and supply its own options type.
- Moved the `OpenAiApiKey`/`SearchApiKey` constants from `BackendEnvironment` onto `BackendContract` (their natural home — they're neutral facts about the contract, not Python-env-var-building logic) and updated `ConformanceFixture.cs`'s one reference (`Realtime.ExpectedApiKey = BackendContract.OpenAiApiKey`) plus doc-comment references in `FakeRealtimeUpstreamServer.cs`.
- **Fixed the real gap item 13 targets**: `new ProcessStartInfo(...).Environment` is pre-populated with a *copy of the current process's entire environment*, not a blank slate — the previous code only overwrote/added the harness's own explicit keys and never stripped anything inherited, so ambient `CONFORMANCE_*`/`AZURE_*`/`*_PROXY`/`VERBOSE_*` vars from the coordinator's shell, a CI runner, or the corporate NuGet/npm/pip proxy setup could leak straight into the Python child process. Added `Conformance.Harness/InheritedEnvironmentFilter.cs`: a standalone, launcher-agnostic `Apply(ProcessStartInfo)` that strips any inherited key matching `CONFORMANCE_*`/`AZURE_*`/`VERBOSE_*` (prefix) or `*_PROXY` (suffix), case-insensitively, then pins `NO_PROXY`/`no_proxy` to `127.0.0.1,localhost` (the backend under test only ever needs to reach the fakes and itself, both on loopback). Wired into `PythonBackendLauncher.StartAsync` *before* the harness's own explicit env vars are applied, so anything the harness deliberately wants set still wins.
- New pure, process-free unit tests (`InheritedEnvironmentFilterTests.cs`, 3 facts) prove the policy directly against a `ProcessStartInfo` object — no backend or fakes needed, deterministic, ~20ms total: mixed-case `CONFORMANCE_*`/`AZURE_*`/`*_PROXY`/`VERBOSE_*` vars removed while unrelated vars (`PATH`, `USERNAME`) survive; `NO_PROXY`/`no_proxy` always pinned to the loopback-only value even when already set to something else; and the pin still happens with no inherited vars present at all. Mutation-checked: temporarily reduced `Apply` to only the `NO_PROXY` pin (removal logic deleted) → clean, specific `Assert.False` failure (`CONFORMANCE_TEST_HOOKS` still present); restored → green.
- Extended `tests/conformance/README.md` with the full `BackendContract` section promised by item 9's placeholder note: every env var the harness sets and why, the environment-stripping policy and its rationale, the exact `/health` response shape (`{status, version, checks}` — read from `app/backend/app.py`'s `_health_handler`, not guessed), and the shared files (`menuItems.json`, `app/backend/static/index.html`) both the harness and the backend depend on.
- `dotnet test tests/conformance`: 35 passed / 1 skipped / 36 total (3 new tests, no regressions — full Python-backed suite still launches correctly with the new stripping in place). 3× green.
- Commit: `09788ed`.

## 2026-09-24 — feat/conformance-harness Stage B item 14 (#7)

- Renamed `app/backend/test_hooks.py` → `app/backend/conformance_hooks.py` (all four production consumers — `order_state.py`, `session_manager.py`, `rate_limit.py`, `rtmt.py` — updated to `import conformance_hooks`) and `app/backend/tests/test_test_hooks.py` → `app/backend/tests/test_conformance_hooks.py`; updated the one remaining doc-comment reference in `Conformance.Harness/BackendProfile.cs`. Grepped the whole repo for `test_hooks` afterward — zero remaining references.
- Rewrote the module docstring to explicitly separate the two independent, previously-conflated mechanisms: `now(tz)` (business-logic clock — only `order_state.py`'s happy-hour pricing reads it) vs. `seconds(env_var, default)` (asyncio timer durations only). Documented the rate-limit hint-clamping caveat in both `conformance_hooks.py`'s and `rate_limit.py`'s docstrings: `CONFORMANCE_RATE_LIMIT_*_DELAY_SECONDS` only overrides the no-hint default fed into `retry_delay(hint, default, bounds)` — the clamp bounds themselves (`FIRST_RETRY_BOUNDS=(0.5,5.0)`, `SECOND_RETRY_BOUNDS=(2.0,8.0)`) are never overridable, so a scripted rate-limit error carrying a "retry after Ns" hint still clamps into production bounds even under `CONFORMANCE_TEST_HOOKS=1`.
- Found and fixed a real gap: `session_manager.py`'s `_RESUME_SWEEP_INTERVAL_SECONDS` (the idle/grace-expiry background sweep loop's interval, default 15s from `config.yaml`) was never wired through the hooks module at all — any scenario needing that background loop to actually fire would have had to wait the full 15s production interval. Added `CONFORMANCE_SWEEP_INTERVAL_SECONDS`.
- Added `_validate_at_startup()`, called once at module-import time: fail-fast (raises immediately, before any business logic runs) if hooks are enabled and `CONFORMANCE_FIXED_NOW` is set but malformed, instead of only surfacing the error lazily whenever some downstream code first calls `now()`; also logs a `WARNING` via `logging.getLogger("sonic-drive-in")` whenever hooks are active, so an accidentally-enabled deployment is loud in its own logs, not just caught by the guard test below.
- Added `TestNeverInInfraOrDockerfile` (new test class): recursively scans `infra/`, `app/Dockerfile`, and `azure.yaml` for the literal substring `CONFORMANCE_` and fails listing the offending file(s) if found — a standing guard against ever wiring these hooks into a real deployment. Mutation-checked: temporarily appended a `CONFORMANCE_FOO` line to `app/Dockerfile` → clean failure listing `app\Dockerfile`; removed it → green.
- Added `TestStartupValidationFailsFast` (4 tests + 1 warning-log test): proves the naive-timestamp and unparseable-timestamp cases now raise during `importlib.reload()` itself (not lazily at first `now()` call), that a valid fixed-now and hooks-enabled-with-no-fixed-now don't raise, and that enabling hooks logs the expected `WARNING`. Mutation-checked by temporarily neutering `_validate_at_startup()`'s body with an early `return` before the parse/log — 3 of 5 tests failed cleanly (`DID NOT RAISE ValueError` ×2, missing warning ×1), the 2 negative-space tests correctly stayed green; restored → all 24 tests in the file green.
- **Fixed a real, subtle cross-file test-isolation hazard the rename exposed**: `test_test_hooks.py` sorted alphabetically *after* `test_order_state.py`, so its `TestHappyHourIntegration` tests' `importlib.reload(order_state)` (which mints a brand-new `SessionIdentifiers` class object every time) always ran *after* `test_order_state.py` had already collected and run against the original class. Renaming to `test_conformance_hooks.py` moved collection order *before* `test_order_state.py`, so the reload's new class object silently diverged from the one `test_order_state.py`'s `from order_state import SessionIdentifiers` had already captured at collection time — `isinstance()` then failed. Fixed properly (not by re-gaming file-name ordering): added `_isolated_order_state()`, loading a private, throwaway copy of `order_state.py` via `importlib.util.spec_from_file_location` under an alias that's never registered as `sys.modules['order_state']`, so happy-hour re-testing never touches the shared canonical module/class the rest of the suite relies on. `order_state.py` does `import conformance_hooks` (a live module reference, not a bound copy), so the private copy still correctly observes whatever the shared, already-reloaded `conformance_hooks` currently reports.
- `app\backend\tests\test_conformance_hooks.py` + `test_order_state.py` run together in collection order: 71 passed. Full `pytest app/backend/tests -q`: **592 passed, 61 subtests passed** (up from the 586 Stage-A baseline; net +6 from this item's new tests). `ruff check .`: clean. `dotnet test tests/conformance`: 35 passed / 1 skipped / 36 total, confirming the Python backend still starts correctly end-to-end importing the renamed module.

